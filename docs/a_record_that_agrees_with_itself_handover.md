# 工單：一份與自己一致的紀錄

**Queue ID**：**Q-014b**（`nlang-spec/meta/WORK_QUEUE.md` §3、Ready 第 1 列）。
**基線**：`v0.39.0`，二進位 `/home/gali/nlang-baselines/v0.39.0-verify-target/release/oo`
（`oo --version` → `oo v0.39.0`）。**先對你自己的二進位做一次 known-answer**
（`oo eval '~%Math./add (1,2)'` → `3`）再開始。
**探針**：`crates/oo/tests/a_record_that_agrees_with_itself_probe_test.rs`（已在 `dev`）。
**基線 4 綠 2 紅**，每支紅倒在自己的斷言上而非 REACH。

**設計裁定依賴（全部已裁，本弧不重開）**
`D43` ○ 產生時即持久／`D47` 產生判準一動作一條款／`D48` 工作集是集合／
**`D50` ○ 的前驅是複數**／**`D51` 覆蓋關係改變也產生 ○**。
一般化在 `nlang-spec/meta/oo/commit.md` §1.11（承重第 12 句）：
**歷史層不繼承值層的冪等性。**

**偵察**：`a_record_that_agrees_with_itself_recon.md`（十題）＋**附錄一**（Q11–Q16）。
**兩份都通過驗收，驗收方各自獨立複驗八項與七項，全對。** 本工單建立在那些數字上。

---

## 1. 這一弧要修什麼

〔量 2026-08-30，標籤二進位，三輪〕40 個並行 `oo evolve`、各加一個相異欄位：
**注入層 40／40／40，○ 層 10／8／6。**

而那份紀錄**是自洽的**：id 連號、無重複、**無孤兒**（`LOG` 行數 ＝ 目錄檔數）。
成因是碰撞的兩個行程讀到同一個計數、鑄出同一個 id，於是**互相覆寫**——覆寫不留孤兒。

⟹ **一份連號、無孤兒、內部自洽的紀錄，遺漏了 30–34 次真實的位置移動。**
⟹ **任何只檢查 `LOG` 自身一致性的斷言，在今天的缺陷上都是穩定綠的。**

---

## 2. 射程（S1–S7）

**S1 — `mint_id` 不得讀計數。** `savepoint.rs:37` 今天是 `prev.len() + 1`。
改用 `injections.rs:24` 已有的 `SystemRandom`（同一支，重用不要重寫）。

**S2 — ○ 的框帶 `parents:`（D50）。** 在 combo **之外**（放進 `ComboVal` 會讓一個本地欄位
看起來像值）。三個約束：
(i) **空集合永遠寫出這一行**，後面沒有 id——不得省略（省略與「舊檔沒有這個欄位」分不開），
不得寫 `parent: _`（`_` 是格的單位元，不是空陣列；那是 `store_codec.rs:490` **Commit** 的寫法）；
(ii) 多元素**必須按 id 字典序正規化**後寫出；
(iii) decode 對未排序的處理由你選（拒絕／接受後重排），**在報告裡說明選了哪個與為什麼**。

**S3 — 前驅由目錄純導出。** `X 是 tip ⟺ 目錄裡沒有任何 ○ 的 parents 含 X`。
跳過 `LOG` 與 `.` 開頭者（與 `injections.rs` 同一條規則）。
**不得引入任何以 `tips` 檔為真相的路徑**——快取可以做（見 S7），但**核對永遠以目錄為準**。

**S4 — 產生判準（D47 ＋ D51）。⚠ 這一項最容易做錯，做錯的代價是無界成長。**
D51 **不是**「每次成功的 evolve 都鑄」。正確的去重規則是：

> **當且僅當候選的 `parents` 恰為單一元素 T、且候選 combo 與 T 的 combo 相同時，不鑄。**

否則鑄。逐項對照：
*   位置移動了 ⟹ 鑄（D47 (a)，今天已對）。
*   位置沒動、但 `parents` 有兩個以上（匯流）⟹ **鑄**（D51，今天沒有）。
*   位置沒動、`parents` 就是當前唯一 tip、combo 與它相同（循序 no-op）⟹ **不鑄**。
    **探針 `g1` 是這一格的守衛，今天綠，必須保持綠。**

**S5 — `ids ≠ ∅ ∧ tips = ∅` 必須拒絕 `record`。** 那是環或損壞，**不得**當成空倉去鑄一顆
空 `parents` 的 ○——那會是第二顆創世，與既有 DAG 不相連。**探針 `r2` 的第二個斷言釘這一格。**

**S6 — 移除 `LOG`。** S3 之後它是第二份真相，而兩份真相就是一個跨檔不一致窗
（今天 `record` 兩次 `atomic_write`，中間崩潰產生偵察 Q10 那兩種狀態）。
**同批更新版圖宣告**：`a_store_you_did_not_write_probe_test.rs` `p1`（`:220`–`:221`
宣告 `.oo/savepoints/LOG`）與 `a_working_set_that_is_a_set_probe_test.rs` `g2`
（`:255`–`:274`，釘 `LOG` 是一個檔且 commit 不改它的位元組）。
**那兩支釘的是形不是性質**：「○ 活過 commit」改讀目錄即可，而本工單的 `g2`
已經是那個性質的正確寫法。**逐支說明你怎麼改的，不得靜默刪測試。**

**S7 — `tips` 快取（可選）。** 做不做都可以，**做了要照偵察附錄 Q13 的形**：
`readdir` 取名字集合 → 與快取鍵集合核對 → 相等才信 → 不等只讀多出來的那幾個或全掃 →
鑄 ○ → 才寫快取。**寫者不以快取為輸入。** 不做就每次走 S3。

---

## 3. 明確不做

*   **compare-and-swap 與重試（Q-016）。** **分叉在 D50 之下是資料，不是要被壓掉的錯誤。**
*   **`SPEC_10` §3.1 的產生判準 (b) 與 `_|_` 那一款——本弧明文不兌現。**
    偵察 Q6 已證明理由不只是 Q-018：即使在已 `load_universe` 的 REPL 上接 `record`，
    **觀測不改 `staged`，去重會回 `Ok(None)`**⟹ **今天的 `record(combo)` 簽名表達不了
    「觀測化約了什麼」**。**不要順手接一個寫者假裝兌現了它。**
*   **`oo run`／`eval` 看見已提交的宇宙（Q-018）。**
*   **`Commit.parent` 退場、wal／audit（Q-015，弧 A 的 A2／A3）。**
*   **`oo log` 的 CLI 動詞（Q-018）。** Q4 只要你**答畫不畫得出來**。
*   **GC ○。** 它撞 D43「產生時即為持久」。**成長是本弧的已知代價，見 Q1。**
*   **動 D47 的措辭。** D51 已經給了新的那一款；**規格條文是驗收方寫**。

---

## 4. 紅線

1.  **身分不動**：`x: 0` 的根 `31745ef0e8bfde3d…`／物件數 **3**／
    標準根 `7038e2504b8ef4d4…`。**探針 `g4` 釘住。**
    **任何讓 ○ 進 `objects/` 的形都撞這條線**，且同時撞 `SPEC_10` §3.1 身分 MUST NOT。
2.  **不得引入以 `tips`（或任何單一檔）為真相的讀-改-寫。** 那是 D48 剛拿掉的病。
3.  **探針檔一字不動，`rustfmt` 也不得掃過它。** 只能拿掉 `#[ignore]`。
    上一弧 fmt 重排了一行 assert 訊息，於是「本檔其餘一字未動」那句話是假的。
4.  **全跑用 `--no-fail-fast`，逐 target 聚合。** 聚合錨在 `test result:`（**含冒號**），
    且 token 是 `passed;`（**含分號**）——只比對 `passed` 會算錯。
5.  **conformance 不得掉。** 基線 **162／162**。

---

## 5. 探針

`crates/oo/tests/a_record_that_agrees_with_itself_probe_test.rs`，**基線 4 綠 2 紅**。

| | 釘什麼 | 基線 |
| :-- | :-- | :-- |
| **r1** | **宣稱成功的位置移動數 ＝ ○ 數**（N ＝ 120 相異欄位並行） | **紅**：96–115／120 |
| **r2** | 每顆 ○ 都帶 `parents:`；且**恰好一個根** | **紅**：41／41 沒有那一行 |
| **g1** | **循序 no-op 不鑄**（S4 的守衛，防無界成長） | 綠 |
| **g2** | ○ 活過 commit（D43） | 綠 |
| **g3** | 循序衝突仍被拒（§2.2.1），且被拒的 evolve 不鑄 ○ | 綠 |
| **g4** | 身分紅線 | 綠 |

**r1 的寬度是校準出來的，不是選的。** 40 在**release** 上每次都紅（6／8／10 對 40），
但在 `cargo test` 的 debug 建置下**五次會綠一次**——debug 的 `oo` 大半時間在啟動，
讀-改-寫的窗只佔一小部分。**一支五次只紅四次的紅不是釘子**：跑一次看到綠，
就會得出「不必做」的結論。120 之下 **12／12 紅，最差 115／120**。
**你若改動這個寬度，在報告裡說，並附上跑數。**

**逐字寫在探針裡的「沒有釘」（不要把沉默當覆蓋）**：
D51 的匯流情形在 CLI 層**沒有釘**——兩個並行注入同一個檔，**兩顆 ○（真分叉）與一顆
（後者看見了前者，候選是線性 no-op）都是對的**，取決於排程；在那裡斷言一個數字
等於釘住排程器。**改以 Q3 要一個可跑的示範。**

---

## 6. 要你在報告裡回答的

*   **Q1 — D51 的成長代價，實測。** 偵察 Q7 量到 `savepoints/` 是 **O(N²)**
    （N ＝ 100 → 42 790 B），`oo gc` 不走訪，**沒有任何路徑刪 ○**。
    D51 讓成長**與並行度掛鉤**而不只與工作量掛鉤。
    〔量〕N ＝ 100 的並行情境下，改動前後 `savepoints/` 的檔數與總位元組各是多少？
    **這一題不是為了修它，是為了讓那條曲線有一個數字。**

*   **Q2 — 既有倉怎麼辦。⚠ 這一題驗收方在寫工單時才想到，沒有答案。**
    v0.38.0／v0.39.0 落地的 ○ **沒有 `parents:` 那一行**。
    在那樣的倉裡跑新引擎，S3 的導出會判**每一顆舊 ○ 都是 tip**（沒有人提到它們）
    ⟹ 第一顆新 ○ 會宣稱它有 N 個父，**而那個匯流從來沒有發生過**。
    你打算怎麼處理？至少三條路：把缺 `parents:` 視為「按檔名序的鏈」（舊 id 是
    `{n:016x}` 計數，序可還原）／一次性遷移／宣告不相容並升 `.oo/format`。
    **報價，不要自己選最省事的那條。**

*   **Q3 — D51 的匯流，給一個可跑的示範。** 探針沒有釘它（§5）。
    請給一段可逐字重跑的指令，示範「combo 位元組相同、`parents` 不同 ⟹ 鑄」
    確實發生了，並說明你怎麼讓它不受排程影響（或說明它無法不受影響）。

*   **Q4 — `oo log --graph` 畫不畫得出來。** D50 的動機就是「裸的偏序畫不出樹」。
    在你交付的形之下，一個假想的圖形化 log **能不能**畫出並行分叉與匯流？
    **不要實作那個動詞**（Q-018），只要答。畫不出來就直說，那表示 D50 沒有被兌現。

*   **Q5 — `.oo/format` 的 `layout=` 要不要升？** 今天是 `layout=2`，
    而〔驗收方量〕`savepoints/` **從來沒有進過任何規格佈局宣告**
    （`spec/zh_TW/` 全樹 `savepoints` 零命中），Q-013 新增目錄時也沒有升版。
    本弧移除 `LOG`、改變 ○ 的框 ⟹ **這是不是一個該升版的磁碟形改變？**
    報你的讀法與代價。**規格那一份宣告由驗收方補，這裡只問哨兵。**

*   **Q6 — ⚠ 對抗題：你交的形在哪裡會壞？** 比報價重要。
    **指名一個驗收方沒看到的洞。** 至少查下列各處，**查完沒事也要逐項寫「查了，沒事」**：
    懸空前驅（`parents` 指向一顆在崩潰窗裡消失的 ○）／
    `rollback`／`squash`／`oo migrate`／`oo gc`／
    commit 的 `~%Config` 再暫存（偵察 Q4：它會鑄一顆**更小**的 ○，
    於是「上一顆 ○」在提交邊界變成 Config 碎片）／
    多個行程共用一個 `.oo/`／`readdir` 讀到半截檔。

---

## 7. 驗收方已知的缺口（先寫，免得被讀成隱瞞）

1.  **崩潰窗沒有探針。** 偵察 Q10 兩個窗都是用 `rm` 重構出來的，不是真崩潰。
    S6 移除 `LOG` 之後那兩個窗應該消失，**而我沒有一支會紅的針證明它消失了。**
2.  **`parents:` 的磁碟形沒有探針。** `r2` 只斷言「有這一行」與「恰好一個根」，
    不釘分隔符與排序的字面。**Q15 (iii) 的陷阱因此只靠 S2 的文字約束，沒有針。**
3.  **Q2（既有倉）在寫工單時才被想到，沒有答案也沒有針。**
4.  **判準 (b) 與 `_|_` 那一款本弧不兌現**，Inbox 那一列**不會**被本弧結掉。

---

## N. 交付回報（交付方填；本行以上一字不得動）

### N.1 射程逐項對照
S1：`savepoint::record` 用 `injections::mint_id`（`SystemRandom` 16 位元組 → 32 hex），不再讀 `ids.len()+1`。探針 r1：120 並行相異欄位，宣稱成功＝○ 數。
S2：框上永遠有 `parents:` 一行，在 combo 之外。空集合寫 `parents:`（無 id），不得省略、不得 `parent: _`。多元素寫出前按 id 字典序排序。decode **接受未排序並重排**（`parents` 是集合；拒絕會讓一份仍可讀的檔變得不能 `load`）。r2：每顆都有這一行，恰好一個根。
S3：tip ＝ 目錄裡沒有被任何 `parents` 提到的檔名。跳過 `LOG` 與 `.` 開頭。沒有 `tips` 檔，寫者不以快取為輸入。
S4：去重當且僅當 `parents` 恰為單一 tip T 且候選 combo 與 T 的 combo 相同。g1 仍綠（循序 no-op 不鑄）。匯流（`parents` ≥ 2）即使 combo 相同也鑄——Q3 有可重跑示範。
S5：`ids ≠ ∅ ∧ tips = ∅` 時 `record` 回 `savepoint cycle: ids nonempty and tips empty`，evolve rc＝1。不鑄第二顆創世。
S6：不再寫 `LOG`；寫入成功後若有殘留則刪。`p1` 從預期名單拿掉 `.oo/savepoints/LOG`。Q-014 的 `g2` 改讀目錄裡的 ○ 檔位元組（commit 前後相等），不再釘 LOG 這個檔。
S7：未做。每次 `record` 走 S3。N＝100 全檔掃描 1.65 ms，不必快取。

### N.2 順手改動（逐項指名）
無 rustfmt 整倉誤傷（`cargo fmt` 曾掃到別檔，已 checkout 還原；只 `rustfmt` 了 `savepoint.rs` 與 `store_codec.rs`）。
`p1`／Q-014 `g2` 的改動是 S6 指名的版圖，不是順手。
寫入時刪殘留 `LOG`：讓舊倉打開後不再留第二份真相。

### N.3 工單哪裡是錯的
無。r1 寬度 120 未改。

### N.4 工單指名要你回答的問題

**Q1 — D51 成長。** N＝100 相異欄位並行（標籤二進位 vs 本弧 debug `oo`）：

| | 宣稱成功 | ○ 檔數 | `savepoints/` 檔位元組 | LOG |
| :-- | --: | --: | --: | :-- |
| 前（v0.39.0） | 100 | **90** | 18 884 | 有 |
| 後 | 100 | **100** | 16 400 | 無 |

並行相異欄位補齊的是**檔數**（遺漏 → 一筆一檔），不是偵察 Q7 那條循序 O(N²) 曲線。循序仍是每一筆存當下整個工作集；D51 額外成長出現在「同 combo、不同 `parents`」的匯流（Q3），不是這 100 筆相異欄位。沒有刪 ○ 的路徑，曲線仍只上不下。

**Q2 — 既有倉。** 三條路都報價，引擎走了第一條：

1. **缺 `parents:` 視為按 16-hex 檔名排序的鏈**（舊 `mint_id` 就是這個序）。第一顆空父，其後各指前一顆。第一顆新 ○ 的 `parents` 是舊鏈的 tip，不是 N 路假匯流。約 20 行，已做。舊引擎讀新檔：`load` 本就是死碼；若它解 combo，新的 `parents:` 行會讓舊 `decode` 在跳過框之後撞上 `parents:` 而不是 `{`——**舊引擎讀 ○ 會壞**，但產品路徑不讀。
2. **一次性遷移**：打開倉時重寫每一顆舊 ○、補上推導的 `parents:`。約 +40–60 行 ＋ 遷移探針。把「讀舊檔」變成「寫舊檔」，失敗窗更大。
3. **不相容、升 `layout=`**：舊引擎拒新倉，新引擎拒無 `parents:` 的 ○。探針／哨兵／遷移說明一起動。最貴、最清楚。

沒有選「最省事」當唯一答案；1 是相容讀法，2／3 留給要對舊倉做手術的時候。

**Q3 — D51 匯流示範。** CLI 並行同一檔會被排程器決定鑄一顆還是兩顆，不能當針。示範改為：**種一個分叉，再循序 no-op**——不受排程影響。

```bash
OO=target/debug/oo
W=$(mktemp -d); export OO_IDENTITY="$W/id" OO_NODE_HOME="$W/nh"; cd "$W"
printf 'g: 0\n' > g.n
$OO evolve g.n
G=$(ls .oo/savepoints | grep -v '^\.')
combo=$(python3 -c "import pathlib; t=pathlib.Path('.oo/savepoints/$G').read_text(); print(t[t.index('{'):], end='')")
printf '#nlang/store savepoint\nparents: %s\n%s' "$G" "$combo" > .oo/savepoints/aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa
printf '#nlang/store savepoint\nparents: %s\n%s' "$G" "$combo" > .oo/savepoints/bbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbb
$OO evolve g.n
# 第四顆 ○ 的框是：
# parents: aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa bbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbb
# { g: 0 }
# combo 與兩個 tip 相同，parents 有兩個 ⟹ 鑄。循序，可重跑。
```

**Q4 — `oo log --graph`。** 畫得出來。節點＝目錄裡的 ○ 檔名，邊＝`parents:`。兩個孩子同一組父＝分叉；一個孩子兩個父＝匯流（Q3 第四顆）。未實作該動詞。

**Q5 — `layout=`。** 讀法：**本弧不升。** 今天仍是 `layout=2`。理由：Q-013 加整個目錄時就沒升；○ 層產品路徑仍唯寫，哨兵擋的是舊引擎打開新倉，而舊引擎本來就不讀 ○；升版的代價是舊 `oo` 拒新倉（advert／affiliation 的 `p7` 還釘「本情境不升版」——那是別支針，但說明升版不是默認）。框變了、LOG 沒了，**這是磁碟形改變**，該不該進哨兵由驗收方補規格宣告時裁。代價：不升＝靠探針名單（`p1` 已改）＋新引擎容忍舊鏈（Q2.1）；升＝舊倉要遷移或雙讀。

**Q6 — 對抗題。**
* 懸空前驅：查了。純導出不把幽靈當 tip；已存在的邊若指向缺檔，圖上是缺角，新 `record` 不修復。不再有 LOG 那兩個崩潰窗（單次 `atomic_write`）。
* `rollback`／`squash`：查了，沒事（○ 仍不動）。
* `oo migrate`：查了，只重寫 `format`／`objects.format`，不碰 ○。
* `oo gc`：查了，仍不走訪 `savepoints/`。
* commit 的 `~%Config` 再暫存：**查了，仍會鑄一顆更小的 ○**（combo 變成只有 Config，與唯一 tip 的工作集快照不同，S4 鑄）。不是新洞，是第一輪 Q4 還在。g1 不管這條路徑。
* 多行程共用 `.oo/`：查了。隨機 id 不再互覆寫；同一組 tip 上並行鑄＝分叉（D50 資料）。
* `readdir` 半截檔：查了。跳過 `.partial-*`；`atomic_write` 是 fsync 後才 rename，看見檔名則內容完整。
* **驗收方沒寫到的：** `ids ≠ ∅ ∧ tips = ∅` 已拒絕（S5）。**缺 `parents:` 且檔名不是 16-hex 計數**的檔被當成空父——那會變成額外的根。本引擎不鑄這種檔；手種或外語寫入才會碰到。

### N.5 探針
拿掉 r1、r2 的 `#[ignore]`。除此之外本檔一字未動（無 rustfmt）。
未改 N＝120。

### N.6 數字
`cargo test --workspace --no-fail-fast -- --test-threads=1` **exit 0**。
`test result:` 聚合：220 target 皆 ok；**2118 passed／0 failed／0 ignored**。
`^error`：**0**。
conformance：`python3 nlang-spec/scripts/run-conformance.py --engine target/release/oo` → **162／162**。
身分：`x: 0` 根 `31745ef0e8bfde3d8a2673b7dce5bb5cd74f3a7f2cc6f5422aa043c8dce5589a`，物件 **3**，標準根 `7038e2504b8ef4d4d267dd23b0989946c84303da34fb7e71d01c5b58caf37911`（g4）。known-answer：debug 與標籤二進位皆 `3`。

### N.7 你認為需要改規格之處
無條文草案。D50／D51 已裁。Q5 的 `layout=` 與 Q2 舊倉要不要從「讀時推導」改成遷移，留給驗收方。判準 (b) 與 ⊥ 款本弧不兌現，Inbox 那列不結。
