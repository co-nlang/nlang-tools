# what the store keeps — Q-001 / W8′-b 偵察

**Queue ID**:`WORK_QUEUE` Q-001(Active)
**基線**:引擎 `v0.19.0`(`oo v0.19.0-534-gf1a06cd`)／規格 `v0.19.0-draft.1`
**這是什麼**:偵察,不是實作工單。目標是把 **O35** 量到「用戶可裁」的程度。
**明確不做**:不改任何程式碼、不改任何測試、不動規格條文。

〔量〕＝本輪實跑的量測。〔重現〕＝ 2026-08-08 W8′ 偵察(引擎 v0.12.0)的結論在 v0.19.0 重測。

---

## 0. 六句承重的話

1. **M1／M2／M3 全部重現,數字幾乎未動**:15 B 的源碼存成 252,435 B 的根物件,其中
   `system` 佔 91%,而 `k1: 1` 仍以**未強制 Thunk 加一份閉包**存入(§1)。
2. **新發現:位址沒有覆蓋它的位元組。** `span` 存進耐久形但**不進雜湊** ⟹ 兩份位元組
   不同的檔案落在**同一個 CAID** 下,而引擎照常服務(§2)。
3. **完整性檢查是存在的,而且會擋**——控制已武裝:一個**合法編碼**的語義竄改被
   `#caid_mismatch` 擋下(§2.2)。所以缺陷不是「沒有檢查」,是**檢查的覆蓋面是儲存
   位元組的真子集**。
4. **M4 已由 v0.13.0 修好,這使 M5 的數字第一次可以當結論**:n/ 形 **21,837 B**,
   且其中 span／closure／Thunk 的出現次數是 **0**(§3)。
5. ⟹ **「存語義值」這個選項的實作已經在樹上了**,它就是印表機。爭點因此不是能不能存,
   **是讀回來要不要求值**(M6／O35 本身,§4)。
6. `system` 26 個模組、七個 minor 之間逐位元組未動 ⟹ M2 的「**這是下一次新增內建的
   風險,不是既成的損害**」在 v0.19.0 仍成立(§1.2)。

---

## 1. M1–M3 重現(引擎 v0.19.0)

〔量〕`app: { k1: 1 }`(15 B)→ `evolve` → `commit`,scratch 倉。

### 1.1 體積與構成

| | v0.12.0(W8′ 偵察) | **v0.19.0(本輪)** |
| :--- | ---: | ---: |
| 源碼 | 17 B | **15 B** |
| 根物件(磁碟,pretty JSON) | 252,587 B | **252,435 B** |
| 緊湊 JSON | 67,981 B | **67,971 B** |
| `system` | 61,922 B | **61,912 B** |
| `data`(使用者資料) | 508 B | **516 B** |

根 Combo 的十個槽,依大小:

```
system    61912      closed        5
types      5186      masa_ref      5
data        516      meta          2
rules       231      local         2
                     relations     2
                     effect        1
```

**使用者資料佔緊湊總長的 0.76%**(M1 的「99.25% 不是使用者的資料」逐位重現)。

### 1.2 `system` 沒有在累積

〔量〕26 個模組(`Math` 11,327／`List` 9,550／`String` 7,989／`Reflection` 4,154…),
與 W8′ 偵察在 v0.12.0 量到的 26 個、61,922 B 相差 10 B。

⟹ M2 結論 (2)「**內建表凍結了,這是下一次新增內建的風險,不是既成的損害**」自
v0.2.55 起經 **十二個 minor** 仍成立。

### 1.3 M3:整數字面值仍以未強制 Thunk 存入

〔量〕`data.app.k1` 的耐久形逐字:

```json
{"Thunk": {
  "expr": {"kind": {"Atom": {"Int": [1,[1]]}}, "span": {"start": 11, "end": 12}},
  "closure": [{"data": {"k1": {"Thunk": {"expr": {…同一個 Atom…}, "closure": []}}},
               "types": {}, "rules": {}, "meta": {}, "system": {}, "local": {},
               "closed": false, "effect": 0, "relations": [], "masa_ref": "Top"}],
  "context": null, "effect": 0}}
```

三件事同時為真,而它們是 (a)(b) 兩問的全部素材:

1. **未強制**——`1` 沒有被求值就存進去了;
2. **帶閉包**——閉包裡是**外層作用域的一份拷貝**,而該拷貝**又含 `k1` 自己**(再一層 Thunk);
3. **帶 span**——源碼位元組偏移 `{11,12}` 是耐久資料。

---

## 2. 新發現:位址沒有覆蓋它的位元組

### 2.1 同一個 CAID,兩份不同的位元組

〔量〕兩個 scratch 倉,唯一差異是源碼多兩個空格(語義相同,span 位移):

| | 源碼 | 存下的 span | **物件位址** | **檔案實際 sha256** |
| :-- | :--- | :--- | :--- | :--- |
| a | `app: { k1: 1 }` | `{11,12}` | `16ba5683…` | `ed95f086…` |
| b | `app: {   k1: 1 }` | `{13,14}` | **`16ba5683…`** | `d478372d…` |

**位址相同,位元組不同。** 機制與 2026-07-29 的身分量測一致:雜湊走
`expr.to_nlang(0)`,而 span 不在 `to_nlang` 的輸出裡 —— **span 進 store,不進雜湊**。

⟹ 對 CAS 而言,`位址 ≠ hash(內容)`。

### 2.2 完整性檢查存在,而且會擋(控制已武裝)

直接竄改磁碟上的物件,三組:

| # | 竄改 | 在雜湊投影內? | 結果 |
| :-- | :--- | :--- | :--- |
| T1 | `span` `{11,12}` → `{9999,10000}` | 否 | **通過**。`oo inspect` 照常回 `k1: 1`,無任何警告 |
| T2 | `Int` `[1,[1]]` → `[1,[2]]`(**合法編碼**,值 = 2) | 是 | **`#caid_mismatch`**,退出碼 1 |
| C | 還原 | — | `k1: 1`,退出碼 0 |

**T2 是必要的控制**:我第一次寫的竄改是 `Int: [2,[2]]`,那是**形狀錯的載荷**——
sign 只能是 −1/0/1,於是它被 serde 擋在 `#object_undecodable`,**證明的是反序列化嚴格,
不是完整性被驗證**。這正是 v0.2.50 立下的規則(對抗項必須含一個會計算的載荷)。
換成合法編碼之後才看見真正的檢查。

⟹ **結論不是「沒有檢查」,是「檢查的覆蓋面是儲存位元組的真子集」。** `to_nlang`
投影之外的一切——至少包含 `span`——是一個**位在內容定址物件內部的未認證區**。

### 2.3 這一半有先例,幾乎自己會裁

O42(v0.17.0)已經為 `#blur` 的身分裁過同一件事:**不收原始碼 span 等讀數**。
把同一條理由套到耐久形,`span` 不該在裡面——它不是內容,是一次讀數。

---

## 3. M4 已修,於是 M5 第一次可以當結論

W8′ 偵察當時記著:`to_nlang` 的收尾是 `_ => format!("{:?}", self)`,Thunk／Code／Ref
三個變體掉進去,所以 22,269 B「**是印不出來的那版的長度**」。v0.13.0(W8′-a)修掉了它。

〔量〕v0.19.0 的三個數字,以及印出來的東西**少了什麼**:

| 形式 | 長度 | 相對 |
| :--- | ---: | ---: |
| JSON pretty(磁碟現況) | 252,435 B | 1× |
| JSON compact | 67,971 B | 3.7× 小 |
| **n/ 形(`oo inspect`)** | **21,837 B** | **11.6× 小** |

而 n/ 形裡 `span`／`closure`／`Thunk` 的出現次數:**0**。`k1` 印出來是 `k1: 1`。

⟹ **印表機今天印的,就是「存語義值」那個選項的成品。** 它不是待實作的設計,它在樹上,
而且它已經丟掉了問題 (b) 列舉的那三樣東西。

**但這同時是一個新的不對稱**:印出來的東西**不能往回**變成磁碟上的東西(M6),
而現在連**磁碟上的東西也不能往回**變成印出來的東西以外的形——因為印表機是有損的。

---

## 4. O35 要可裁,還缺什麼

O35 的原文:**`.oo/` 的耐久形要不要是可求值的?**

三個選項與各自代價(costs 為本輪量到或由量測直接推出):

| | 耐久形 | 讀取 | 代價 |
| :-- | :--- | :--- | :--- |
| **A** | 值形子語言,不可求值 | 解碼,不求值 | **需要一個 decoder,今天不存在**(M6)。**CAID 全面失效**(M3:`1` 強制前後 digest 不同)⟹ 破壞性 + 遷移 |
| **B** | 維持可求值 | 剖析 → 求值 | **讀取即執行**,與 disc 028 §8 供應鏈正面相撞。且 §2 的未認證區成為可藏東西的位置 |
| **C** | 值形為主,惰性以**明示標記**保留 | 解碼;僅標記處延後 | 需要先裁「哪些能進身分」(問題 b),否則標記本身會進 CAID |

**本輪把 (a) 的答案量出來了**:store 今天存的是**求值狀態**,而**語義值那一版已經存在**
(印表機,21,837 B)。所以 (a) 不再是未知,它是一個選擇。

**仍缺、且我不打算替你決定的**:

1. **惰性要不要活過重啟。** 若耐久形不可求值,`k1: 1` 就得在存入前強制;那麼「無限
   結構」「未觀測即不計算」在跨越 `.oo/` 邊界時是什麼意思,需要一句話。這是 SPEC_04 §6
   的射程問題,不是儲存格式問題。
2. **`span` 可以單獨先裁**(§2.3 有 O42 的先例),而且它比整個 O35 便宜得多——
   它只需要「span 不是內容」這一句,不需要決定惰性。

---

## 5. 本輪順帶量到、但不屬於本弧的

| # | 東西 | 建議去處 |
| :-- | :--- | :--- |
| 1 | `#caid_mismatch` 的訊息把 **requested 印成 `v1` 格式、recomputed 印成 `v2` 格式**(後者含一大段 base64 sketch)⟹ **人無法把兩邊對起來**。與「說得出為什麼」同族 | `WORK_QUEUE` Inbox |
| 2 | O47(吸收不得改寫快照)結案狀態仍未確認——`WORK_QUEUE` 補漏時標為「開 Q-001 前順手確認」,本輪未做 | 仍掛著 |

---

## 6. 下一步

本檔到此為**偵察**,依協定不含實作。裁定 O35 之後才產生 implementation 工單(Q-010)。

**要用戶裁的,依便宜程度排**:

1. **`span` 是不是耐久內容**(有 O42 先例,可單獨裁,不阻塞其他)。
2. **O35 本身**:A／B／C 三選一。
3. 若選 A 或 C:**惰性活不活過重啟**(§4.1)。
