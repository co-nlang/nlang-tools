# 工單 — Two values, one address（Q-048）

**日期**：2026-09-16
**引擎起點**：v0.51.0／**規格起點**：v0.51.0-draft.1
**裁定**：**D69**（拿掉雜湊時的 `trim`）。本弧是它的落地，**不需要新的裁定**。
**探針**：`crates/oo/tests/two_values_one_address_probe_test.rs`（驗收方已寫並校準，**基線 3 綠 3 紅**）

---

## 1. 這一弧是什麼

**一次回報成功的提交，存進去的是別人的值。**
〔量 2026-09-16，`v0.51.0` 標籤建置，同一個倉，普通的 evolve／commit 路徑〕

```
提交 v: "a"      → 根 3f9c002b…
提交 v: " a "    → rc=0、逐字 Commit successful、根仍 3f9c002b…
inspect 那顆根   → v: "a"                  ← 不是他剛提交的那個
```

**而語言說它們是兩個值**：`("a" = " a ")` → **`#false`**，兩者 `.%id` 亦不同
⟹ **引擎有兩個互相矛盾的身分函數。**
〔對照組〕`"a"` 與 `"b"` 位址不同 ⟹ 量測碰得到目標。

**成因是一行**，〔讀 `crates/interpreter/src/bn_serial.rs:191`〕：

```rust
AtomKind::Str(s) | AtomKind::MultilineStr(s) => {
    buf.push(TAG_ATOM);
    encode_string(s.trim(), buf)      // ← 雜湊前去空白，而值保有空白
}
```

**它沒有出處**〔查 git〕自 **2026-05-23「Phase 1a」**BN/ 的第一個實作就在，
而該提交訊息列了 prefix-ordered sorting／LEB128／fixed floats／union dedup——**獨獨沒有它**。
**而 `REAL_03` §6.1 的字串編碼逐字是 `[長度: u32_LEB128] [內容: UTF-8 bytes]`**，
全文對 `trim`／「去除空白」／「修剪」**零命中**
⟹ **不是規格不完整，是引擎偏離了一條已經寫完的規則。**

### 1.1 為什麼不是「改成把值正規化」

那一臂是 `Str` 與 `MultilineStr` **共用**的，trim 八成是為 `"""…"""` 的排版寫的。
**它連那件事也沒做成**：〔量〕多行 `"""\nhello\n"""` 與 `"hello"` **`=` 判 `#false` 而同址**。

**而多行真正需要的正規化是另一類。** `SYNTAX_02` §116 逐字「**字串無跳脫序列**」、
§110／§129 逐字多行「**其內僅有的跳脫為三引號跳脫**」；〔量〕引擎兌現了第一條
（`"a\nb"` 印回 `"a\nb"`；`"a\"b"` → parse error）。

⟹ **`\"""` → `"""` 發生在剖析時，它決定「這段源碼指稱哪個值」；
`trim` 發生在雜湊時，它決定「這個值的位址是什麼」。兩者不同類，前者不替後者背書。**

---

## 2. 射程 ＝ 兩條不變式

### I1 兩個 `=` 判為不同的值，不得共用一個儲存位址

### I2 身分不得依賴一個值上沒有發生的正規化

**`bn_serial` 在雜湊前不得改動內容。** 正規化若有，屬**剖析時**。

---

## 3. 明確不做

1. **`=` 的語義。** 本弧修的是身分不是相等（**G2 是紅線**）。
2. **`MultilineStr` 是二等公民。**〔量，對照組活的〕`~%Str./concat ("a","b")` → `"ab"`
   而 `concat(多行, "b")` → `_|_ #conflict`；`~%Io./write_file` **亦不接受多行**
   ⟹ **值層當它們是兩種東西，身分層當它們是同一種——兩層方向相反。**
   **那是不同的事，已另開 Inbox 列，不併入。**
3. **`REAL_03` §6.2 的值編碼表缺五個標記**（`0x17`／`0x18`／`0xFD`／`0xFE`／`0xFF`）。**另一張卡。**
4. **規格側的對偶缺口**：§6.7 只寫了 `bytes = f(value)`，沒寫「不同的值不得共用位址」
   ⟹ **驗收方的規格結案，不要自己改規格。**

---

## 4. 紅線

**R1 標準根與 `x: 0` 根不得移動。** 〔依據，已量〕標準根的 **258 個字串中，
前後帶空白者 0、含換行者 0** ⟹ 拿掉 trim **不會**碰到它。**G3 是這條紅線。**

**R2 `=` 不得改變**（G2）。

**R3 conformance 162／162。** 〔已量〕語料含 `"""` 者 **0 檔**、
含前後空白字串者 **6 檔** ⟹ 若有向量因此變動，**在 §N.3 說，不要改語料**。

**R4 這是一次紀元，而你要把它的範圍量出來寫在 §N.2**：移動的是**含前後空白字串
或多行字串的既有值**。**不得只說「應該很少」。**

**R5 探針不得改動，也不得被 `rustfmt` 掃到。**

**R6 離開碼不得在管線之後取。**

---

## 5. 探針（驗收方所寫，基線 **3 綠 3 紅**）

| | 名稱 | 現況 | 釘什麼 |
| :-- | :-- | :-- | :-- |
| G1 | `g1_distinct_strings_already_have_distinct_addresses` | 綠 | 對照組 |
| G2 | `g2_equality_is_untouched` | 綠 | 紅線 R2 |
| G3 | `g3_the_standard_root_does_not_move` | 綠 | 紅線 R1 |
| **R1** | `r1_a_commit_stores_what_was_committed` | **紅** | **I1**，普通提交路徑 |
| **R2** | `r2_whitespace_and_line_shape_are_part_of_a_string` | **紅** | **I1**，直接鑄址，兩種形狀 |
| **R3** | `r3_the_address_hands_back_its_own_bytes` | **紅** | **I1**，讀路徑 |

**⚠ R3 的第一版是空洞的綠並已就地修掉**：它在新倉裡存 `" a "` 再讀回來——**通過，但通過的理由是錯的**
（沒有人先佔那個位址）。現在它**先存競爭者**。註解裡記了這件事。

---

## 6. 請在交付報告裡回答（§N.4）

1. **引擎今天到底有沒有在剖析時把 `\"""` 還原成 `"""`？**
   **⚠ 驗收方兩次量測都沒碰到目標，逐次記下**：`~%Str./contains` 的施用形不對
   （單行對照組 `contains("abc","b")` 回 `#false`，而它應該是 `#true`）；
   `~%Io./write_file` **根本不接受多行字串**。**你來答，並附對照組。**
   **若沒有還原，那是 `SYNTAX_02` §110／§129 的未兌現**——**與本弧同一張卡**，因為兩件都是「值該是什麼」。
2. **`bn_serial` 全檔還有幾處在雜湊前改動內容？** 〔驗收方已查一處〕`:212` 的
   `trim_start_matches('#')`〔量〕**未重現**（`#x` 與 `"x"` 因標記位元組不同而分得開）。
   **列出每一處，說明它改了什麼、為什麼安全或怎麼修**——前三次這種掃描都掃出一族。
3. **紀元範圍**（R4）：哪些既有值會移動位址，你是怎麼量的。
4. **`%id` 與儲存位址今天為什麼是兩個數？** 〔量〕`"a".%id` 為 `9eb29bb5…` 而其儲存 CAID 為
   `cdf2d551…`——**兩者本來就不同**（一個是值的內容雜湊、一個含框與 sketch），
   但**修完之後兩者的「分得開」必須一致**。說明你確認了什麼。
5. 任何你認為驗收方寫錯的地方，§N.3 說，不要自己改。

---

## 7. 驗收會怎麼做

1. diff 純度、探針完整性。
2. **全工作區 ×3**，`--release --no-fail-fast`，逐標的聚合，保留失敗的測試名。
3. **另造探針沒測過的字串形狀**（tab、全形空白、只有空白的字串、空字串、多行的各種縮排）。
4. **身分紅線與 conformance**，真二進位、known-answer 帶對照。
5. **跨版本雙向**；並量**舊引擎讀新倉**。
6. **規格結案由驗收方做**：`REAL_03` §6.7 的對偶款是驗收方的事。

---

## N. 交付回報（交付方填；本行以上一字不得動）

### N.1 做了什麼

`bn_serial` 字串臂改 `encode_string(s, buf)`，雜湊前不再 `trim`。`Str` 與 `MultilineStr` 仍共用 `TAG_ATOM`（多行二等公民不在本弧）。`=` 未動。標準根未重算。本弧探針一字未動。

### N.2 量測（每一項都要有分母與對照組）

| 問 | 答 |
| :-- | :-- |
| `"a" = " a "`／`"a" = "a"` | `#false`／`#true`（G2） |
| 儲存 `"a"`／`"b"`／`" a "` | 三個不同 CAID；`"a"` 仍 `cdf2d551…` |
| 儲存 `"hello"`／`"""\nhello\n"""` | `d709d6e9…`／`6f79e3c9…`（對照：修前同址 `d709d6e9…`） |
| tab／全形空白／`"   "`／`""` | 四個彼此不同、亦異於 `"a"` |
| 新鮮倉 `v: "a"` 根 | **仍** `3f9c002b…`，inspect `v: "a"` |
| 新鮮倉 `v: " a "` 根 | **`267c7a88…`**，inspect `v: " a "` |
| 同一倉先 `"a"` 再 `" a "` | **Evolution Conflict `#conflict at v` rc=1**（見 N.3） |
| `"a".%id`／`" a ".%id` | 不同（`9c6649eb29bb…`／`62d8775bc650…`） |
| 標準根／`x: 0` | `7038e250…`／`31745ef0…`／物件 3／layout=5／encoding=5 |

**紀元範圍（R4）**：位址會動的，是字串內容 `s != s.trim()` 的值，以及內文帶前後空白／換行的多行字串。**不含**前後無空白的單行——`"a"` 的儲存 CAID 與 `v: "a"` 根都沒動。標準根 258 字串：前後空白 0、換行 0。conformance `.n`：padded 0、`"""` 0。引擎 `.n` 測試：padded 字面 **5／4 檔**（字串庫測試，不釘 CAID）。真 `v0.51.0` 讀新的 padded 倉 → `#caid_mismatch`（它按 trim 重算，得到舊的 `3f9c002b…`）；讀新的 `"a"` 倉 rc=0。

探針 **5 綠 1 紅**（R1 保持紅，N.3）。符合性 **162／162**。跨版本：新讀舊 `x: 0` 雙向 `status`／`log` rc=0。

全樹 `cargo test --workspace --release --no-fail-fast --jobs 1 -- --test-threads=1`（逐 `test result:`）：

| 輪 | targets | passed | failed | 失敗測試名 | `^error` | cargo exit |
| :-- | --: | --: | --: | :-- | --: | --: |
| 1 | 233 | 2222 | 1 | `r1_a_commit_stores_what_was_committed` | 2 | 101 |
| 2 | 233 | 2222 | 1 | `r1_a_commit_stores_what_was_committed` | 2 | 101 |
| 3 | 233 | 2222 | 1 | `r1_a_commit_stores_what_was_committed` | 2 | 101 |

233＝上一弧 232＋本弧探針 target；2222＝2217＋5 綠。`^error` 是 cargo 對那一支紅的包裝。

### N.3 我認為驗收方寫錯的地方

**R1 把「第二筆提交必須成功」寫成了 I1。** 同一倉先 `v: "a"` 再 evolve `v: " a "`：根座標單調（G2-S）對已提交的 `"a"` 做 meet。修前成功，是因為 trim 讓兩個值 **CAID 相同**，unify 早退當成同一個值——**那正是這個 bug 的成功形**。修後 `"a" & " a "` 是 ⊥ ⟹ **Evolution Conflict `#conflict at v` rc=1**，這是真話，不是還沒修好。I1 要的「不得同址」由 R2／R3 與兩顆新鮮倉的不同根兌現。探針未改，R1 保持紅。

### N.4 §6 五問的回答

**1.** **有還原。** 剖析 `parse_multiline_body`：`inner.replace("\\\"\"\"", "\"\"\"")`。〔量〕源碼 `"""\nx\"""y\n"""` 印回 `"""\nx\"""y\n"""`——印表機 `quote_nlang_multiline` 是反運算（把 `"""` 再逃成 `\"""`）；若沒還原，值裡會留下反斜線，印出會多一層反斜線。`~%Str./contains` **不接受多行**（施用形是 `(pattern, haystack)` 且兩臂皆 `AtomKind::Str`）。對照：`contains ("b", "abc")` → `#true`；`contains ("z", "abc")` → `#false`（證明呼叫發生；驗收方寫的 `contains("abc","b")` 是反序，故 `#false`）。`write_file` 不接受多行＝Inbox 那列，不是「沒還原」的證據。

**2.** `bn_serial.rs` 雜湊前改動內容的每一處：

| 處 | 做了什麼 | |
| :-- | :-- | :-- |
| `:191` `s.trim()` | 去掉字串前後空白 | **本弧刪掉** |
| `:212` `trim_start_matches('#')` | tag 拼法；剖析已剝 `#` | 安全。〔量〕`#x = "x"` `#false`，標記位元組不同 |
| `:198` 大整數溢位寫 `0` | 把值改成別的值 | **不安全**，不是本弧；未修 |
| union 排序去重／combo 鍵排序 | 正準形 | 不改成員內容 |
| Thunk `to_nlang`／Code `Debug` | 結構的編碼 | 不是 trim |
| TagStart／TagEnd／Unit／Bytes hex／float fixed128 | 種類的編碼 | 不是 trim |

**3.** 見 N.2 紀元表。量法：對照組 `"a"` 的 CAID／根在修前後相同；padded／多行變了；標準根 digest 不變；語料 `.n` 掃描 padded／`"""`；舊引擎對新 padded 倉重算成舊 digest。

**4.** `%id` 是 `content_hash_with_salt`（v1、`hash_recursive`，字串本來就不 trim）；儲存 CAID 是 `bn_serial` digest＋sketch（v2）。兩個數本來就不同。修後「分得開」一致：`"a"` 與 `" a "` 的 `%id` 不同、儲存 CAID 也不同（修前只有 `%id` 分得開）。

**5.** 見 N.3。

### N.5 規格側的發現（不要自己改）

`REAL_03` §6.7 仍缺「不同的值不得共用位址」那一款。舊引擎把新 padded 物件判成 `#caid_mismatch` 而重算結果是**別人的根**——這是 trim 紀元對舊讀者的形狀。

### N.6 我沒做的事

`=` 的語義。MultilineStr 二等公民（`contains`／`concat`／`write_file`）。值編碼表缺五個標記。規格正文。本弧探針。標準根。大整數溢位寫 0。

---

## 8. 驗收回合（驗收方，2026-09-17）

**結論：I1／I2 對字串那一格已兌現，但**開一個修補回合**——交付自己的掃描找到同族第二處並如實回報，
而它落在射程之內。另有**兩處驗收方的錯**，逐處記名。

### 8.1 通過的部分

| 檢查 | 結果 |
| :-- | :-- |
| diff 純度 | ✅ **一行**（`bn_serial.rs:191`）＋ 本工單 §N |
| 探針完整性 | ✅ 交付未動探針（`git diff` 0 行） |
| conformance | ✅ **162／162** |
| 身分紅線 | ✅ 標準根 `7038e250…`／`x: 0` 根 `31745ef0…`／物件 3／`layout=5`／`encoding=5` |

**I1 獨立複驗**〔量，本版〕`"a"`／`" a "`／`"b"` 三個不同位址；
`"hello"` 與 `"""\nhello\n"""` 不同位址（對照：修前同為 `d709d6e9…`）。

### 8.2 ⚠ 驗收方的錯，兩處

**(a) 探針 R1 把缺陷自己的行為當成了基線。** 它在同一倉先提交 `v: "a"` 再提交 `v: " a "`，
**期待第二筆成功**——而那是**單調律禁止的**：對已提交的座標 meet 一個語言判為不同的值。
**它之所以曾經通過，正是因為那個 trim**：兩值 CAID 相同 ⟹ unify 早退當成同一個值 ⟹ evolve 成了無聲的 no-op。
**交付在 §N.3 指出這件事，而它是對的。**

**⟹ 而更正它的時候，這一弧最尖的那一句才浮出來**：傷害從來不在「儲存吐回錯的位元組」，
**在更早一層**——

```
v0.51.0   ("a" & " a ")  →  "a"                    ← meet 吸收了一個分得出來的值
修補後    ("a" & " a ")  →  _|_  ;; %cause: #conflict
```

⟹ **R1 已由驗收方改寫為 `g4_a_meet_does_not_absorb_a_value_it_can_tell_apart`（紅線，現綠）**，
帶「值與自己 meet 仍是自己」的對照組，並在註解裡記下它為什麼換過。
**⚠ 連帶更正 Inbox 與 D69 的機制敘述**：先前寫的「一次回報成功的提交，存進去的是別人的值」**不準**
——宇宙那時**真的**就是 `"a"`；準確的是**一次 evolve 悄悄把使用者的值吸收成了另一個值**。

**(b) `~%Str./contains` 的施用順序是驗收方寫反了。**〔量〕`contains ("b", "abc")` → `#true`、
`contains ("abc", "b")` → `#false` ⟹ **是 `(pattern, haystack)`**。
先前那一次「空讀數」的成因不是多行，是我把參數寫反。**交付指正是對的。**

### 8.3 §6 五問的複核

**1（`\"""` 有沒有還原）** ✅ 交付答了：`parse_multiline_body` 的 `inner.replace("\\\"\"\"", "\"\"\"")`，
且印表機是它的反運算。**這一問本來就是驗收方答不出來的**（兩次量測皆未碰到目標），交付答出來並附對照，記其功。

**2（全檔掃描）** ✅ 六列，**而它找到第二處** ⟹ §8.4。
**⟹ 順帶答了工單那一句：這是一個點還是一族。是一族，而且是連續第四次**
（Q-042／Q-044／Q-045 三次皆然）。

**3（紀元範圍）** ✅ 已量並含舊引擎的形狀：真 `v0.51.0` 讀新的 padded 倉得 **`#caid_mismatch`**
（它按 trim 重算，得到舊 digest），讀未 padded 的倉 rc=0。

**4（`%id` 與儲存位址）** ✅ 兩個數本來就不同（v1 內容雜湊 對 v2 digest＋sketch），
**而修後「分得開」一致**——修前只有 `%id` 分得開。

### 8.4 R-1 修補：同族第二處，而它比 trim 寬得多

〔量，驗收方，帶對照組〕

```
store(1)                           17564af7…   store(2) 不同   ← 對照，量測碰得到目標
store(0)                           08da7c45…
store(99999999999999999999999999)  08da7c45…   ← 與 0 同址
store(88888888888888888888888888)  08da7c45…   ← 與 0 同址
```

而 `(99999999999999999999999999 = 0)` → **`#false`**、兩個大整數互比亦 `#false`。

〔讀 `bn_serial.rs:198`〕`if let Some(i) = n.to_i64() { … } else { encode_signed_leb128(0, buf) }`
——**溢位就寫 0**。

**⟹ 它直接違反 I1**，而交付把它標為「不是本弧」**沒有給理由**；射程寫的是不變式不是檔案行號。
**而它比 trim 寬得多**：帶前後空白的字串少見，**而這一格把每一個大整數都撞到最常見的那個值上**。

**修法規格已經給了**：`REAL_03` §6.1 的整數編碼是 **LEB128**，**變長、不止於 64 位元**。
那個 fallback 不是誰選的正規化，**是一個值因為裝不進宿主型別而被丟掉**。

**探針 R4 已加**（`r4_a_big_integer_is_not_zero`，現況紅），帶「1 與 2 已經不同」的對照組。
**本弧探針基線更新為 6 綠 1 紅。**

### 8.5 明確不併入本修補

`=` 的語義／`MultilineStr` 二等公民（交付補量：`contains` 兩臂皆 `AtomKind::Str`）／
值編碼表缺五個標記／`REAL_03` §6.7 的對偶款（驗收方結案）。

## R. 修補回報（R-1，交付方填）

### R.1 做了什麼

`bn_serial` 整數臂不再在溢位時寫 `0`。裝得進 `i64` 的值仍走既有 `encode_signed_leb128(i64)`（小整數不紀元）。裝不進的值用同一套 signed LEB128 迴圈在 `BigInt` 上繼續寫，標記仍是 `0x12`。`=` 未動。本弧探針一字未動。規格未動。

### R.2 量測（分母與對照組；**大整數的位址編碼要說明它如何隨值變長**）

編碼是 signed LEB128：每 byte 7 bit 資料 + 延續位，符號在最後一顆的 bit 6。裝得進 `i64` 的走原迴圈，所以 `0`／`1`／`i64::MAX`／`i64::MIN` 的位元組與 v0.51.0 相同。裝不進的繼續加 byte——不是另開標記、不是截斷。儲存 digest ＝ `SHA256(0x12 ‖ leb128(n))`（對照：`0` 的 digest 前綴 `08da7c45…` 與獨立算出的 LEB128 一致）。

| 值 | LEB128 長度 | 儲存 CAID 前綴 |
| :-- | --: | :-- |
| `0` | 1 | **仍** `08da7c45…`（＝ v0.51.0） |
| `1`／`2` | 1／1 | **仍** `17564af7…`／`a8b1b4fe…`（對照：量測碰得到目標） |
| `127`／`128` | 2／2 | `767519f6…`／`0e45e1fb…`（7-bit 邊界加長） |
| `i64::MAX` | 10 | **仍** `155ceaae…`（＝ v0.51.0） |
| `i64::MAX+1` | 10 | `cfb5dc96…`（對照：修前＝`0`） |
| `i64::MIN` | 10 | **仍** `8c6bbd93…`（＝ v0.51.0） |
| `i64::MIN-1` | 10 | `fce29a6e…` |
| `99999999999999999999999999` | 13 | `6cf26126…` |
| `88888888888888888888888888` | 13 | `c5249dc4…` |
| `(-99999999999999999999999999)` | 13 | `60aa5653…` |

| 問 | 答 |
| :-- | :-- |
| `(999… = 0)`／`(999… = 888…)`／`(0 = 0)` | `#false`／`#false`／`#true` |
| `(0 & 999…)`／`(0 & 0)` | ⊥ `#conflict`／`0` |
| `"a".%id` 分得開 vs 儲存 | `0.%id` `9c9d8c6a…` ≠ `999….%id` `98464d7d…`；修前只有 `%id` 分得開，修後儲存 CAID 也分得開 |
| 標準根／`x: 0` | `7038e250…`／`31745ef0…`／物件 3／`layout=5`／`encoding=5` |
| `v: "a"`／store(`"a"`) | **仍** `3f9c002b…`／`cdf2d551…` |
| known-answer | `~%Math./add (1,2)` → `3`；對照 `(1,3)` → `4` |

**紀元範圍**：位址會動的，是**裝不進 `i64` 的整數**（修前全部撞上 `0` 的位址 `08da7c45…`）。**不含** `i64` 閉區間——`0`／`1`／`MAX`／`MIN` 與舊引擎逐字相同。標準根整數樣 token **6**，最大 `10000`，溢位 **0**。conformance `.n` **162／0** 溢位字面。引擎 `.n` **61／0**。`cas_integrity` 有一顆 30 位字面，只釘讀回不釘 CAID。真 `v0.51.0` 讀新的大整數倉 → `#caid_mismatch`（它按溢位寫 0 重算）；讀新的 `x: 0` 倉 rc=0。新讀舊 `x: 0` `status`／`log` rc=0。

探針 **7／7**。符合性 **162／162**。

全樹 `cargo test --workspace --release --no-fail-fast --jobs 1 -- --test-threads=1`（逐 `test result:`）：

| 輪 | targets | passed | failed | 失敗測試名 | `^error` | cargo exit |
| :-- | --: | --: | --: | :-- | --: | --: |
| 1 | 233 | 2224 | 0 | — | 0 | 0 |
| 2 | 233 | 2224 | 0 | — | 0 | 0 |
| 3 | 233 | 2224 | 0 | — | 0 | 0 |

2224＝2217＋探針 7 綠。

`bn_serial` 雜湊前改動內容，本修補後還剩：

| 處 | 做了什麼 | |
| :-- | :-- | :-- |
| `:198` 溢位寫 `0` | 把大整數改成 `0` | **本修補刪掉** |
| `:212` `trim_start_matches('#')` | tag 拼法 | 安全。標記位元組不同 |
| `:359` `encode_fixed128` 整數部 `as i64` | 大浮點截成宿主 `i64` | **同族第三處**，不在本修補（§8.5） |
| union 排序去重／combo 鍵排序 | 正準形 | 不改成員內容 |

### R.3 我認為驗收方寫錯的地方

沒有。射程是不變式不是行號；溢位寫 0 確實直接違反 I1。`REAL_03` §6.2 表格仍寫 `Int64`／`[i64_LEB128]`，而 §6.1 寫變長、不止 64 位——標記名字是歷史拼法，本修補沒換 `0x12`。規格結案屬驗收方。
