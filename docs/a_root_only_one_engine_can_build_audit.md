# 偵察複驗 Q-033：只有一台引擎造得出的根

> 2026-08-19，驗收方複驗。基線與偵察相同：`nlang-tools dev e1c018b`、
> `nlang-spec local 559277b`、`oo v0.26.0-618-ge1c018b`。
> 樹在複驗期間未改動；本檔不改規格、不改引擎。

---

## 0. 總評

**偵察可採信。**§3 的每一個數字獨立重測後逐格相同（137,028／68,506／21,913 bytes、
26 模組、256 引用／251 相異、前綴表 26 列、`#io` 20／`#state` 5／`#nondet` 1）。
§2、§5、§6、§7 的結構性結論也成立。**這位代理是真的量了，不是推的。**

但複驗翻出**一則反例、一則層級誤判，以及一個偵察沒看到的引擎缺陷**——
最後一項改變 D1／D2 的形狀，且是 Q-032 的回歸。

---

## 1. 錯誤一：`@list.mine` 不是可定義的（偵察 §4.1 末段五名中的一名）

偵察寫：「Q-032 解封的是使用者座標：使用者可定義 `@int.mine`、`@unit.mine`、
`@u8.mine`、`@list.mine` 甚至 `@zzz.mine`。」

**五名中四名為真**（`@int`／`@unit`／`@u8`／`@zzz`），錯的是 `@list`。
`@zzz` 是探針的偵查哨兵慣例，用來表達任意未知名，該處用法無誤。
但錯的那一名**正好落在爭點集合裡**——三個標準根型別，見 §11。

〔量〕四個標準根座標**全部仍封著**，只有控制組通：

| 式 | 結果 |
| :--- | :--- |
| `@zzz.mine`（控制組） | `9` |
| `@int.mine` / `@unit.mine` / `@u8.mine` | `1` / `2` / `3` |
| `/add.mine` | `⊥ #missing_key` — Key 'mine' missing in closed Cocoon |
| `@list.mine` | `⊥ #missing_key` |
| `@option.mine` | `⊥ #missing_key` |
| `@result.mine` | `⊥ #missing_key` |

`@list` 那一格另有 E4 規則（`type_constraint.rs:45`，12 個保留名不可遮蔽）作為
既有解釋；**`/add` 沒有**——它不是型別名。所以這不是型別模型問題。

---

## 2. 缺陷：投影路徑的查找順序是反的（Q-032 回歸，偵察未見）

〔量〕同一個名字、同一份原始碼，三種用法拿到**兩個不同的值**：

```nlang
/add: { mine: 1 }
app: {
  bare_add: /add        ;; → { mine: 1 }        使用者的
  applied:  /add(1, 2)  ;; → ⊥ #conflict        使用者的(無 %builtin)
  proj:     /add.mine   ;; → ⊥ #missing_key     標準根的閉合繭
}
```

**裸解析與應用走使用者的根；只有投影掉進標準根。**

原因在兩處查找的順序相反，且**同出於 Q-032 交付 `a71a69b`**：

| 路徑 | 順序 | 位置 |
| :--- | :--- | :--- |
| 裸名 | scopes → `~%Config` → staged → **`ctx.root`** → **`ctx.standard_root`** | `lib.rs:3673` → `:3700` |
| 投影 | scopes → `~%Config` → staged → **`ctx.standard_root`** → **`ctx.root`** | `lib.rs:3813` → `:3840` |

裸名那處還帶著寫對的註解：「They are not hydrated into the user root, so a user
definition above wins.」**投影那處把同一句話做反了。**

工單 §2.1 對此有明文：「root 先、標準根後……**不得改變這個方向**。」

### 2.1 寫得進身分，讀不回來

〔量〕提交 `/add: { mine: 1 }` 後，根物件**確實含有**該欄位、確實進了雜湊：

```
{ /add: { mine: 1 }  @list: { mine: 2 }  @zzz: { mine: 9 }
  app: { v: 1 }      ~%__nlang_system_digest: "2da5b713…" }
```

而寫下它的那台引擎讀不回 `/add.mine`。**這是「持有但打不開」（Q-031）在座標粒度
上的同一個形狀**，只是這次不是拒絕，是靜默地回答了別人的值。

### 2.2 為什麼 P1 是綠的卻沒見證到

`the_half_that_was_never_written_probe_test.rs:174` 的 P1 只斷言 `evolve` 不含
`Error`。`evolve` **確實**成功——失敗發生在觀測。而 O58 要的「四個孤兒變成可遮蔽」
**是觀測性質**。

> **教訓（本次新增）：斷言「沒有報錯」的探針，只見證了沒有那個報錯。**
> 與本弧已記的兩則同族（P3 綠而無見證；斷言訊息子字串＝釘拼法）。
> 這是驗收方的漏，探針由我寫並校準。

---

## 3. 錯誤二：`~%Official` 不是待裁項，是已裁而未兌現（偵察 §5 誤判層級）

偵察把 `~%Official` 列進 D2「逐案處理，不得以『都先保留』代替裁定」。
但 SPEC_13 §135 **已經裁了**，並寫了理由：引擎本地合成的同名節點
「不是該物件的退化版本，而是**另一個東西冒用其名**」，且指定了正解——
本地觀測該路徑應得 `⊥ #missing_key`。

〔量〕現值不是那個：

| 式 | 結果 |
| :--- | :--- |
| `~%Official` | `{{ }}`，CAID `27c48162…` |
| `~%Official.anything` | `⊥ #missing_key` |
| `~%NoSuchModule`（控制組） | `_` |

路徑本身解析成一個**被合成出來的空物件**，不是 `⊥`。這是現行 MUST NOT 的違反，
不是待裁項。

**耦合（偵察未指出）**：這個空殼在標準根的 26 個模組裡 ⟹ **它在每一個根的身分裡**。
移除它會移動標準根 digest ⟹ v0.26.0 之後那是破壞性身分變更。
**因此它不能當獨立清理做，必須搭 D2 的 B 或 C 一起切，否則身分代價付兩次。**

---

## 4. 補強：規格唯一一張核心清單，引擎只兌現三分之一

偵察 §5 量的是一個方向（引擎有 14 個模組規格沒有規範清單）。反方向沒量。

〔量〕SPEC_09 §5.1「核心系統組件清單」是規格裡唯一一張規範性模組表，六列：

| 規格列 | 引擎 |
| :--- | :--- |
| `~%Logic` | **缺席（`_`）** |
| `~%List` | 存在 |
| `~%Str` | **缺席**（引擎叫 `~%String`；`~%Str` 全規格僅此 1 處，`~%String` 0 處） |
| `~%Option` | **缺席** |
| `~%Result` | **缺席** |
| `~%Cond` | 存在 |

`option`／`result` 只以**型別**存在於 types 軸，以及 `option.`／`result.` 的
builtin 前綴——**沒有同名模組**。

**這改變 D2 的代價表**：偵察說 A（凍結 v0.26.0 全量）「最便宜」。
但 A 會蓋章一個**連規格自己的核心清單都不滿足**的標準根 ⟹ A 必須同時重寫 §5.1，
否則不自洽。A 沒有偵察說的那麼便宜。

---

## 5. 補強：§6 的 round-trip 障礙是 3 個欄名，不是 printer

偵察結論「由引擎印一份 `.n` 放進規格目前不可用」成立，但範圍可以收得更緊。

〔量〕標準根共 **265 個相異欄名**，其中**不是合法識別字的恰好 3 個**：

```
system / Engine / rules / %differential.1
system / Engine / rules / %differential.2
system / Engine / rules / %differential.3
```

全部在 `~%Engine` 一個模組裡，形狀是 arity 多載（`.1`／`.2`／`.3`）。
**這是引擎內部多載編碼漏進使用者可見座標**，不是 printer 的結構性缺陷。

⟹ D3 不必然要設計一套 escaping：先裁那三個座標的正準拼法，
round-trip 的障礙就從「printer 不可用」縮成「一個模組的命名慣例」。

---

## 6. 逐項複驗總表

| 偵察章節 | 判定 |
| :--- | :--- |
| §2 兩個 L3 同名誤會；runner 寫死 L1／L2 | ✅ 成立（`run-conformance.py:23` `['L1','L2']`；corpus 僅 L1 78／L2 208；介面僅 `oo run --observe`） |
| §3 標準根盤點全部數字 | ✅ 逐格重測相同 |
| §4.1 `@list` 同名雙身分 | ✅ 成立，且**比偵察更強**（見 §7） |
| §4.1 末段 使用者座標已解封 | ❌ **推翻**（本檔 §1） |
| §4.2 兩張規格表與現樹未對齊 | ✅ 成立，且漏了 §5.1（本檔 §4） |
| §5 名稱反例（`Str`／`System`／`Logic`／`Repl`／`Official`） | ✅ 量測成立；`Official` 的**層級誤判**（本檔 §3） |
| §6 printer 不可 round-trip | ✅ 成立，範圍可收緊（本檔 §5） |
| §7 L3 向量最低契約五項 | ✅ 採納 |
| — 投影查找順序反轉 | ❌ **偵察未見**（本檔 §2） |

---

## 7. §4.1 比偵察說的更重：這是 §6.7 的第三例

偵察把雙身分呈現為 D1 的設計選項。〔量〕但它同時違反 SPEC_04 §156 的
「顯示、`=`／`<=`、CAID **四者同調**」：

| 式 | 結果 |
| :--- | :--- |
| `@list` 顯示 | `{{ %kind: #type, %name: "list" }}` |
| `(@list)` 顯示 | `{{ %kind: #type, %name: "list" }}` — **相同** |
| `@list <= (@list)` | `#true` |
| `(@list) <= @list` | `#true` — **雙向** |
| `@list.%id` | `c596dc99…` |
| `(@list).%id` | `2a43f297…` — **不同** |

**顯示相同、雙向 refine、CAID 不同。**四者裡兩者說同一個值、兩者說兩個值。

這正是 v0.26.0 剛關掉兩次的 §6.7 形狀（等值不等位元組）的**第三例**。
⟹ **D1 不是自由選擇**：現況兩種讀法都不自洽，無論怎麼裁都有既寫下的東西要改。

---

## 8. 對 D1–D4 的影響

| 題 | 複驗後的變化 |
| :--- | :--- |
| **D1** | 範圍要擴大。`/add` 證明雙重解析**不是型別現象**；且 §7 顯示它已是條文違反，不是偏好。**但先修 §2 的順序反轉再裁**——那可能吃掉 D1 的一半 |
| **D2** | A 不便宜（§4）。`~%Official` 不進 D2，它已裁；只需決定**搭哪一次 digest 移動**（§3） |
| **D3** | 障礙比偵察小（§5）。先裁 `%differential.n` 拼法 |
| **D4** | 不變，§7 五項採納 |

---

## 9. 建議的下一步順序

1. **先修 §2 的投影順序反轉**——單點、不動規格、不移動標準根 digest，
   且它是 Q-032 未兌現承諾的機制。**修完再重量 §1 的四個座標與 §7 的雙身分**，
   因為那可能改變 D1 的題目本身。
2. 順帶補一支**觀測**（非 `evolve`）的探針，把 P1 的漏補上。
3. 之後才進 D1–D4 的裁定。

**狀態：偵察已複驗，可採信；新增一則引擎缺陷與兩則更正，建議在裁定前先修 §2。**

---

## 10. §2 已修（2026-08-19，驗收方直接修，未開單）

**理由**：方向在 O58 工單 §2.1 已裁（「不得改變這個方向」），交付把它做反了。
這不需要新裁定，開單只是把已裁事項再繞一圈。

保留協定中有價值的那一半：**探針先寫，先對未修改的二進位校準成紅**。

### 10.1 改動

`crates/interpreter/src/lib.rs` 投影路徑：`ctx.standard_root` 與 `ctx.root` 兩個
區塊對調，並補一段說明兩條路徑必須同向。**產品碼 diff 僅此一處**（15 增 11 刪）。

### 10.2 探針 `crates/oo/tests/a_name_that_resolves_two_ways_probe_test.rs`

| | 基線 | 修後 |
| :--- | :--- | :--- |
| C1 自由名投影得到使用者的值（harness 對照） | 綠 | 綠 |
| C2 未遮蔽時標準根仍投影得到 | 綠 | 綠 |
| C3 裸名已是使用者的值 | 綠 | 綠 |
| C4 E4 保留驗證器不被削弱 | 綠 | 綠 |
| R1 `/add.mine` 可觀測 | 紅 | 綠 |
| R2 `@list`／`@option`／`@result` 座標可觀測 | 紅 | 綠 |
| R3 一個名字不得有兩種解析 | 紅 | 綠 |
| R4 根物件持有的讀得回來 | 紅 | 綠 |

**四支紅每一支都觀測**，不是「`evolve` 沒報錯」。C4 刻意存在：E4 管 `&` 驗證那條軸，
本次不得碰。

### 10.3 驗收

*   `cargo test --workspace --no-fail-fast` **×5：1972 passed / 0 failed / 0 ignored
    （202 套件），五次全同**。基線 1963/1（201 套件）＋本檔 8 支＋1 套件 ⟹ **零回歸**。
*   符合性向量 **143／143**。
*   **標準根 digest 未動**：`2da5b713…`；`app: { v: 1 }` 的根位址 `426d5186…`
    修前修後逐字相同 ⟹ **身分不移動**，patch 形狀（先例 v0.24.1）。
*   對 `/home/gali/nlang-baselines/v0.26.0-verify-target` 的真二進位並排：
    四個座標由 `⊥ #missing_key` 變為 `1／2／3／4`，控制組 `9` 兩版相同。

### 10.4 副產物：一個既有危害，本次擴散了範圍

〔量〕去前綴 fallback 讓 **ctx.root 的模糊比對贏過標準根的精確比對**：

```nlang
app: { add: 7, use: /add(1, 2) }   ;; use → ⊥ #conflict，/add 解析到兄弟欄位 7
```

**對 v0.26.0 真二進位並排：兩版逐字相同 ⟹ 這是既有的，不是本次修出來的。**
它住在裸名路徑上——Q-032 那一處順序寫對了，但同樣讓模糊比對outrank精確比對。

**但本次修正把它從裸名路徑擴散到投影路徑**（`app: { add: /add.mine }` 此前落在
標準根得 `⊥ #missing_key`，此後落在兄弟欄位）。兩條路徑同向是本次的目的，
危害隨之同向是代價。

⟹ **不併入本次射程**（那是兩件不同的事：本次治 root／標準根的**順序**，
該項治**精確比對與模糊比對的優先序**，且需裁定「`add:` 是否本來就該遮蔽 `/add`」）。
**進 Inbox。**

建議的順序若裁定要修：root 精確 → 標準根精確 → root 模糊 → 標準根模糊。

### 10.5 規格側待收（切版時處理）

1.  **`spec/CHANGELOG.md:46-48`（v0.26.0-draft.1）「順帶關閉四個孤兒……拆開後
    四者皆可遮蔽」是假的**——寫入成功而觀測不成立。同 §6.8.1 那次的更正形狀，
    必須誠實記下：該句自 v0.26.1 起為真。
2.  **`REAL_03:468` 自陳缺口第一項**（「使用者因此無法在自己的宇宙頂層定義 `/add`」）
    本次解除，可劃掉。第二項（SPEC_09 §2.1／§2.5 兩表不一致）留著——那是 D2。


---

## 11. `()` 為什麼會改變結果（2026-08-19 用戶提問，量後補）

**不是括號的事，是段數。**

〔碼〕`crates/interpreter/src/lib.rs:3561`：

```rust
if path.anchor == PathAnchor::Bare && path.segments.len() == 1 {
    …   // :3582  E4 marker 短路（12 個保留名）
    …   //        root → 標準根 查找
    …   // :3726  未知名 → Unknown marker
}
self.resolve_path_internal(path, ctx)   // 多段一律走這裡
```

**整套 marker 機制只住在「單段」分支。** `@list.%fmap` 是**一條兩段的路徑**，
從頭到尾沒碰過 `is_type_constraint_path`，直接進 `resolve_path_internal` 沿標準根
走到豐富節點。`()` 只是逼 `@list` 單獨解析完，於是它落進單段分支拿到 marker，
`.%fmap` 才變成對「已算完的值」做投影。

〔量〕`%name`／`%kind` 兩種寫法皆通、`%fmap` 只有無括號通——**前者的一致是巧合**
（marker 恰好複製了豐富節點的這兩個欄位），不是同一條機制。

### 11.1 「雙身分」只發生在 12 名中的 3 名

〔量〕12 個保留驗證器名，各問 `@X.%id`（多段）與 `(@X).%id`（單段）：

| 名 | `@X.%id`（多段） | `(@X).%id`（單段） |
| :--- | :--- | :--- |
| `any num complex float int str bool combo morphism`（**9 名**） | **`_`** | 有 CAID |
| `list option result`（**3 名**） | 有 CAID | 有 CAID，**且不同** |

**若雙角色是設計，它會適用於 12 名。它只適用於 3 名——而那 3 名正是 D2 可能
拿掉的那 3 個。** 重疊不是型別模型造出來的，是**標準根的清單**造出來的。

### 11.2 更難看的一格：豐富節點沒有任何表達式指得到它

〔量〕`@list`（單段）恆為 marker；豐富節點的**欄位**可達（`@list.%fmap`），
**節點本身不可達**。而 `@list.%id` 報的正是那個不可達之物的位址。

⟹ 這不是「一個名字有兩個值」，是「一個名字有一個值，外加一個只能被 `%id`
指認、卻無法被取得的影子」。SPEC_04 §156 的「四者同調」在此不只是不成立，
而是**其中一者根本沒有對應的可觀測值**。

### 11.3 E4 的「不可遮蔽」比它的註解窄

〔量〕`type_constraint.rs:45` 註解稱保留名「不可遮蔽」，但那只在**單段**成立：
`@int & 10` 為 10（E4 綠），而 `@int.mine` 在 v0.26.0 之前就已回使用者的值。
**「不可遮蔽」是某一種觀測形的性質，不是名字的性質**——偵察 §4.1 末段對此的
判斷是對的。

### 11.4 對 D1 的建議

**不要照題目裁 D1。**〔量〕重疊集合恰為「保留名 ∩ 標準根 types 軸」＝ 3 名，
而後者由 D2 決定 ⟹ **D1 是 D2 的下游，先裁會裁到一個 D2 可能取消的題目**。

*   D2 若把 `list`／`option`／`result` 移出標準根 types 軸 ⟹ 12 名一致，D1 自動消失。
*   D2 若保留 ⟹ 才需要裁「marker 是不是豐富節點的截斷投影」。
    〔量〕marker 的欄位集 `{%kind, %name}` 是豐富節點 `{%kind, %name, %fmap}`
    的**真子集** ⟹ 「同一個值的兩個收斂深度」是可辯護的讀法，
    但**必須讓單段也回豐富節點**，否則 §11.2 的影子不會消失。
    代價：`@list` 的 CAID 由 `2a43f297…` 移至 `c596dc99…`，
    而使用者可以提交 `x: @list` ⟹ **身分軸破壞**。
