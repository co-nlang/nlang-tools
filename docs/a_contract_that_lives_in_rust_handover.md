# 工單 —— 一份住在 Rust 裡的合約（Q-039）

> **交付方請先讀完本檔再動任何一行。** 本檔描述**一弧的封閉射程**，不是 backlog。
>
> 基線：`nlang-tools dev 1e3c752`（乾淨）／`oo v0.36.0`／規格 `v0.36.0-draft.1`。
> 〔量 2026-08-26，`--no-fail-fast`〕`targets=215 passed=2078 failed=0 ignored=0 err=0`；
> conformance **158/158**。
>
> 偵察：`docs/a_contract_that_lives_in_rust_recon.md`（**必讀**，本檔不重述其量測）。
> 裁定：`nlang-spec/meta/oo/STATUS.md` **O76**／**O77**／**O78**。
> 探針：`crates/oo/tests/a_contract_that_lives_in_rust_probe_test.rs`
> ——**基線 7 綠 8 紅**，已由驗收方校準（每一支紅都倒在它自己的斷言上，不是倒在 REACH）。

---

## 0. 一句主張

**內建繞過了語言自己的施用機制，於是把它做對的兩件事各自重做了一遍，而兩遍都做錯。**

*   **入口**：內建接受哪些鍵，只以 Rust 閉包裡的 `get_field("0")` 存在。沒有任何東西宣告它，
    所以**角色互換的呼叫通過了形狀檢查並被真的施用**。
*   **出口**：`list.rs` 的六行以**印出形**判真，於是「謂詞塌了」「謂詞說不」「謂詞回了 5」
    是同一格，而**字串 `"true"` 算成原子 `#true`**。

---

## 1. 射程

### S1 —— 內建必須宣告它綁定哪些鍵（O76 ③、O78 ②）

宣告**住 registry，不進值**。〔理由，O78 ②〕內建的值多一個欄位會移動標準根 digest，
而標準根 ⊕ 進每一個根的身分（O58）⟹ 那是一次紀元，本弧不付。

宣告**必須是一件規格事實**：`SYNTAX_09` §2 #1 已寫下這條 MUST 並附未兌現宣告。
交付要做的是讓它兌現——形狀由你選，但**必須能回答「這個內建收哪些鍵」而不必執行它**。

〔偵察已量，可直接用〕241 個註冊點中 **207（85.9%）是固定 arity 的純位置形**，鍵集可由現有
原始碼機械讀出；34 個「無 `get_field`」走共用 helper（`get_two_args` 8 處、`extract_str_arg`
11 處）或不吃引數（`math.random` 逐字 `|_arg|`）；7 個具名。

### S2 —— 未宣告的鍵形不得被靜默接受（O76）

**失配路徑已經存在且集中**：〔讀 `lib.rs:3138-3170`〕內建回 `Value::Top` 時，施用層自動建出
部分施用的繭。**這一弧不需要新造失配機制**，只需要在呼叫 `func` 之前多一次宣告比對。

**探針 r4** 釘的是性質不是拼法：失配後的結果**不得是一個看起來合理的答案**。
繭、`⊥`、其他，都可以；`0` 不可以。

### S3 —— 標準庫統一（O78 ①③）

**正準判準（O78 ③，本弧採用）**：

> **凡意圖被 `|>` 使用的態射，其資料參數必須是最後一個。**

〔理由，已量〕`|>` 把被管的值放進**下一個空位**（`lib.rs:3049` 逐字
`nf.insert((max_idx + 1).to_string(), arg.clone())`）⟹ 管道只與「資料放最後」組合得起來。

**要動的**（探針 r1／r2／r3／r7／r8 各釘一格）：

| 現行 | 內建 | 動作 |
| :--- | :--- | :--- |
| list-first | `~%List./take_while`、`./drop_while` | 改為資料在最後 |
| list-first | `~%Query./where` | 同上。**⚠ 見 §3 的 L2-117** |
| 嗅探 | `~%List./filter`、`./map` | **拿掉嗅探**，宣告一種 |
| 第五種形 | `~%List./fold` | 見 §5 的必答題 |

**⚠ 射程含全部 40 個 `~%List` 註冊點**，不只上表這幾個。偵察只量到已知的四群，
**你必須逐個列出現行形**並在 §7.1 交出那張表——否則「統一」這件事沒有人知道做完了沒有。

**O78 ① 的彈性條款要照字面讀**：一致性的**粒度是模組**，「同一個模組底下，相似型的態射
**傾向**使用同一種」。⟹ 若某個內建的現行形是**必須**（有結構理由，不只是習慣），
**不要動它**，把理由寫進 §7.3。**判準已經有一條可證偽的**（管道），用它，不要用美感。

### S4 —— 迭代型內建不得把 ⊥ 判為假（O77）

〔偵察已量〕整個面**只有六行**，全在 `list.rs`：`:351`（`filter`）／`:458`（`any`）／
`:485`（`all`）／`:513`（`find`）／`:653`（`count`）／`:715`（`partition`）。

〔對照，不要一起改〕另外 **11 處** `trim_start_matches('#')` 是**安全的**——它們先
`match AtomKind::Tag(t)`，`⊥`／字串／數字根本不進那條臂。**動它們是順手改動，須在 §7.2 指名。**

`SPEC_08` §4.3 的新款**刻意不要求中止**，要求的是**可分辨**。形狀由你選，
探針 r5 只釘「塌陷 ≠ 說不」。

### S5 —— `#true` 與字串 `"true"` 分家（同一行程式碼）

與 S4 同批，不獨立處理。探針 r6。

---

## 2. 紅線

1.  **不動身分。** 根 `932a9f9d…`／標準根 `7038e250…`。**宣告不得進入內建的值**（O78 ②）。探針 g5。
2.  **conformance `L2-104` 不得改。** O76 明文允許具名鍵；那七個具名內建（`engine.project_down`
    `target`/`masa`、`project_up` `sections`、`set_strategy` `strategy`、`check_oml` `a`/`b`、
    `diff.patch` `path`/`to`、`query.select` `path`、`disc.find` `target`）**不是要改成位置式，是要宣告**。探針 g2。
3.  **不動 `unified_arg` 的組成**（`lib.rs:3027-3074`）。那是 2026-07-25 已裁並修好的。探針 g4。
4.  **不動 curry 與位置式 combo 的等價**。探針 g3。
5.  **不動 `~%Cond` 的分支表語義**，只要它宣告。探針 g6。

---

## 3. conformance：三支向量，三種待遇

| 向量 | 待遇 |
| :--- | :--- |
| **L2-104** | **紅線，一字不得改**（見 §2）。 |
| **L2-116** | **由驗收方改寫為資料在最後。** 安全——〔量〕兩種引數序**都得 `⊥ #effect_violation`**，它釘的主題與引數序無關。探針 g7 已把改寫後的形釘成今日為真。 |
| **L2-117** | **⚠ 危險，必須與 `~%Query./where` 的實作同批處理。** 〔量〕list-first → `1`（正確）／pred-first → **`0`**。**若你改了 `where` 卻沒改這支向量，它會從 `1` 變成 `0` 而仍然綠**——那正是本弧要治的病。**在 §7.1 明說你怎麼處理它。** |

**補向量是本弧的一部分**：〔量〕`SPEC_09` 對 `~%List` 只有一行（§394「張量處理」），
引數形**0 條文 0 向量**。至少要有：一支釘「宣告的形答對」、一支釘「未宣告的形不靜默答」、
一支釘「塌陷可分辨」。

---

## 4. 明確不做

*   **不裁「哪一種順序是正準的」作為語言規則**——O76 已定：由態射宣告。S3 是**標準庫家規**，層級不同。
*   **不改 apply 層的失配機制**（回 `Value::Top` → 繭）。它是對的，用它。
*   **不動任何位址、不動標準根內容、不加 `ComboVal` 欄位。**
*   **不處理 `~%Cond./cond`／`./match` 的分支表該長什麼樣**（`SPEC_09` §5.2 與實作意見不同那件事）
    ——本弧只要它**宣告它收什麼**。條文對齊是驗收方的規格收尾。

---

## 5. 工單指名要你回答的問題

1.  **`list.fold` 的現行協定與你選的協定，逐字寫出來。** 〔偵察，讀 `list.rs`〕它取**兩個**鍵、
    **嗅探**哪一個是清單，另一個必須是 `{ %val: 初值, %f: 態射 }`。
    〔量〕驗收方**用盡了想得到的拼法都到不了它**——記錄形的兩種順序都得 `⊥ #conflict`。
    **⟹ 本項沒有探針，這一句是逐字聲明，不是遺漏。** 你要在 §7.4 給出一個**可跑的呼叫**。
2.  **那 34 個「無 `get_field`」的宣告掛在哪裡？** helper 上（一次覆蓋多個）還是註冊點上？
    `math.random` 這種「不吃引數」怎麼宣告？
3.  **有沒有哪一個內建的現行形是「必須」不是「習慣」？** 有就別動，把結構理由寫下來（O78 ① 的彈性條款）。

---

## 6. 交付自檢（缺一項即退回）

*   [ ] 全跑 `cargo test --workspace --no-fail-fast` **×5**，逐字相同，且與基線比對過增量
      （增量必須等於「解除的紅 ＋ 你新增的測試」，差一支都要解釋）。
*   [ ] conformance 全綠，且**明說你動了哪幾支、為什麼**。
*   [ ] 探針檔 **`git diff` 只有移除 `#[ignore]`**。若你認為某支釘錯了，**寫進 §7.3，不要改它**。
*   [ ] **順手改動逐項指名**（含 `cargo fmt` 的重排、含你認為明顯是改善的）。
*   [ ] 身分紅線：用**真二進位**量根 CAID 與標準根 digest，貼出來。
*   [ ] §7 七個小節**皆不得留空**；沒有的寫「無」，答不出來的寫「答不出來，因為……」。

---

## 7. 交付回報（交付方填；本行以上一字不得動）

### 7.1 射程逐項對照

S1 …
S2 …
S3 …（含 40 個 `~%List` 註冊點的現行形對照表，與 L2-117 的處理）
S4 …
S5 …

### 7.2 順手改動（逐項指名）

### 7.3 工單哪裡是錯的

### 7.4 工單指名要你回答的問題

1. `list.fold`：
2. 34 個無 `get_field` 的宣告：
3. 「必須」而非「習慣」者：

### 7.5 探針

### 7.6 數字

### 7.7 你認為需要改規格之處
