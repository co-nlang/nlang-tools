# 工單 — A type you could not carry（Q-047）

**日期**：2026-09-16
**引擎起點**：v0.50.0／**規格起點**：v0.50.0-draft.1
**裁定**：**D68**（Q-033 的 D1 取乙）。本弧是它的落地，**不需要新的裁定**。
**偵察**：`nlang-tools/docs/a_root_only_one_engine_can_build_recon.md` §10.3／§10.8
**探針**：`crates/oo/tests/a_type_you_could_not_carry_probe_test.rs`（驗收方已寫並校準，**基線 3 綠 2 紅**）

---

## 1. 這一弧是什麼

`@list` 看起來是一個名字，行為像兩個東西，**而你拿到哪一個取決於你怎麼拼這個式子，不是取決於那個值**。

〔量 2026-09-16，v0.50.0 標籤建置〕

```
@list.%fmap       {{ %builtin: "list.map" … }}      ← 標準根的節點
(@list).%fmap     _
t: @list ⟹ t.%fmap        _                        ← 綁起來就沒了
m: { t: @list } ⟹ m.t.%fmap  _                     ← 放進 combo 也沒了
```

⟹ **門是純詞法的**：`@name.` 在投影期對標準根解析，而 **`@name` 這個值永遠是約束 marker**。
**沒有任何可綁定的東西帶著那顆節點。**

**而那個詞法形遮蔽 marker，不是擴充它**——對三個有節點的名字它走得更遠，
**對其餘每一個它什麼都拿不到，連 marker 明明有的欄位也拿不到**：

| | `@X.%name` | `(@X).%name` |
| :-- | :-- | :-- |
| `list`／`option`／`result` | ✅ | ✅ |
| `int`／`num`／`str`／`bool`／`any`／`float`／`combo`／`record`／`zzz` | **全部 `_`** | ✅ |

**佐證**：`SPEC_05` §3.2 自己的例子逐字寫的是 **`(@int).%name` → `"int"`**——**規格寫的是那個會動的拼法。**

---

## 2. 射程 ＝ 兩條不變式

### I1 一個名字一個答案

**`@X.f` 與 `(@X).f` 必須給出同一個答案，對每一個型別名字。**
`@X.f` 是對 `@X` 這個值的**普通取欄位**，**不得**是一條會遮蔽 marker 的名字查找。

### I2 型別是一個帶得走的值

**把 `@X` 綁到名字、放進 combo、傳遞之後，它仍然是同一個值。**
〔今天〕`t: @list` 之後 `t.%fmap` 為 `_`。

**⟹ 兩條合起來就是 D68 的「單一值」**：`@X` 同時承載 marker 的欄位與標準根節點的欄位。

---

## 3. 明確不做

1. **`%super` 的語義**。它依 `SPEC_05` §3.2 與裁定 R1 行事，**G2 是這條紅線**。
2. **`@zzz` 的開放性**。D68 讀乙 為「沒有節點的名字仍是型別值，只是沒有 `%fmap`」
   ——**那是推論不是另裁**。本弧維持開放；**若你發現它站不住，在 §N.3 說，不要自己改**。
3. **D3′（manifest 的規範形）與 D4（runner 契約）**。兩者皆未裁；
   **且 D3′ 有它自己的前置**——`REAL_03` §6.2 的值編碼表缺五個標記
   （`0x17` Thunk／`0x18` Range／`0xFD` `#blur`／`0xFE` `_|_`／`0xFF` `_`），
   **而標準根自己需要 `0xFF`**。與本弧無關，記在此處免得誤併。
4. **不公佈任何 CAID；不重算標準根；不動 `SPEC_09` §2.1／§2.5 那兩張表**
   （`SPEC_09` §2.5 註解逐字要求在該題裁定前不要動它們——**而 D68 正是那一題**，
   故**改那兩張表是驗收方的規格結案，不是你的**）。

---

## 4. 紅線

**R1 非紀元。** 標準根 `7038e250…`、`x: 0` 根 `31745ef0…`、物件 **3**、`layout=5`、`encoding=5`
皆不動；conformance **162／162**。**G3 是這條紅線。**
〔依據〕`%super` 是**衍生反映欄**，〔量〕標準根裡出現 **0 次** ⟹ 合成單一值**不需要存它**。

**R2 已經會動的那一側不得變壞。** `(@X).f` 今天對十二個名字全部有答案（**G1**）。

**R3 `%super` 不得被搬家或改語義**（G2）：`(@int).%super.%name` → `"num"`、
`(@any).%super` → `_`、**手造的 `{ %kind: #type, %name: "int" }` 仍須拿不到 `%super`**。

**R4 探針不得改動，也不得被 `rustfmt` 掃到。** 認為探針寫錯 ⟹ §N.3 說明並保持它紅。

**R5 離開碼不得在管線之後取。**

---

## 5. 探針（驗收方所寫，基線 **3 綠 2 紅**）

| | 名稱 | 現況 | 釘什麼 |
| :-- | :-- | :-- | :-- |
| G1 | `g1_the_marker_answers_for_every_name` | 綠 | 紅線 R2（十二個名字） |
| G2 | `g2_super_still_reflects_the_type_tree` | 綠 | 紅線 R3（三格，含手造 combo 的反例） |
| G3 | `g3_no_address_moves` | 綠 | 紅線 R1 |
| **R1** | `r1_both_spellings_of_a_type_name_answer_the_same` | **紅** | **I1**（12 名字 × 2 欄位，逐格列出不一致） |
| **R2** | `r2_a_type_survives_being_bound` | **紅** | **I2**（綁定、以及經一層 combo；帶 live 對照） |

---

## 6. 請在交付報告裡回答（§N.4）

1. **那一格驗收方沒有量的**：乙 需要讓標準根那顆節點**也算是一個型別值**
   （今天衍生只認引擎鑄的 marker——G2 的第三格就是證據）。**你做了什麼，它牽動了什麼？**
2. **`@list` 這個值現在的 CAID 是什麼？** 是 `c596dc99…`（節點）、`2a43f297…`（marker），
   還是**第三個**？若是第三個，**明確回答它有沒有移動任何既有位址**。
3. **型別值進不進得了根與歷史？** 〔量〕`v: @list` 提交之後根裡是什麼、物件數是多少。
4. **`=` 的行為有沒有變？** 〔今天〕`@list = (@list)` 為 `#true`，而那是 marker 比 marker。
5. **全函數**：對 `SPEC_09` §2.1 樹裡的每一個名字、以及不在樹裡的名字，
   `@X.f` 各給什麼？**沒有落點的那一類回什麼？**
6. 任何你認為驗收方寫錯的地方，§N.3 說，不要自己改。

---

## 7. 驗收會怎麼做

1. diff 純度、探針完整性（`git diff` 為空、`rustfmt` 未掃）。
2. **全工作區 ×3**，`--release --no-fail-fast`，逐標的聚合，保留失敗的測試名。
3. **另造探針沒測過的名字與欄位**，檢查 §6.5 的全函數宣稱。
4. **身分紅線與 conformance**，以真二進位、known-answer 帶對照。
5. **跨版本雙向**；並量**舊引擎讀新倉**時型別值長什麼樣。
6. **規格結案由驗收方做**：`SPEC_09` §2.1／§2.5 兩張表與 `SPEC_05` §3.2 的按語
   是驗收方的事，**不要自己改規格**。

---

## N. 交付回報（交付方填；本行以上一字不得動）

### N.1 做了什麼

**I1。** 多段 `@X.f` 在名字找不到時改鑄 marker 再取欄位，不再對 Top 投影。`@int.%name` 與 `(@int).%name` 同答。單段 builtin 短路只留給**不在標準根上的**保留名（E4：`@int & 10` 仍走 marker）；`@list`／`@option`／`@result` 落入普通查找，值就是那顆已存在的節點。

**I2。** `t: @list` 與 `m: { t: @list }` 綁到的是節點，`t.%fmap`／`m.t.%fmap` 與字面 `@list.%fmap` 同答。

**§6.1 那一格。** 新增 `is_type_value_combo`：閉合 ＋ `%kind: #type` ＋ `%name`（不論有沒有 `%fmap`）。衍生 `%super`、meet、`≤` 認它。手造開 combo `{ %kind: #type, %name: "int" }` 仍不是型別值（G2 第三格）。兩欄 marker 形 `is_type_constraint_combo` 留給巢狀 marker 掃描。標準根未重算。

### N.2 量測（每一項都要有分母與對照組）

| 問 | 答 |
| :-- | :-- |
| `@list`／`(@list)` | 同為節點 `{{ %kind: #type %name: "list" %fmap: … }}` |
| `@list.%fmap`／`(@list).%fmap` | 同為 `list.map` 繭 |
| `@int.%name`／`(@int).%name` | 同為 `"int"` |
| `@zzz.%name`／`@zzz.%fmap` | `"zzz"`／`_`（開放，沒有節點） |
| `@list.%super.%name`／`(@list).%super.%name` | 同為 `"any"`（節點現在算型別值） |
| 手造 `{ %kind: #type, %name: "int" }.%super` | `_`（G2） |
| `@list = (@list)` | `#true` |
| `@list = {{ %kind: #type, %name: "list" }}` | `#false`（節點 ≠ 兩欄 marker） |
| `@list.%id` | `c596dc99…`（既有節點，不是第三個位址） |
| `1 & @int`／`@int & "x"` | `1`／`_|_ #conflict` |
| 提交 `v: @list` | 根裡是節點；物件 **3**；標準根 digest 仍 `7038e250…` |
| `@int: { hacked: 1 }` 後 `@int & 10` | `10`（E4） |

探針 **5／5**。`type_super` 14／14。

身分：`eval '~%Math./add (1,2)'` → `3` rc=0（對照 `(1,3)` → `4`）。`x: 0` 根 `31745ef0…`／標準根 `7038e250…`／物件 3／layout=5／encoding=5。

符合性：**162／162**。跨版本真 `v0.50.0` 雙向 `status`／`log` rc=0；舊引擎 inspect `v: @list` 倉看見同一個節點（資訊沒有掉成 marker）。

全樹 `cargo test --workspace --release --no-fail-fast --jobs 1 -- --test-threads=1`（逐 `test result:`）：

| 輪 | targets | passed | failed | 失敗測試名 | `^error` | cargo exit |
| :-- | --: | --: | --: | :-- | --: | --: |
| 1 | 232 | 2217 | 0 | — | 0 | 0 |
| 2 | 232 | 2217 | 0 | — | 0 | 0 |
| 3 | 232 | 2217 | 0 | — | 0 | 0 |

232＝上一弧 231＋本弧探針 target；2217＝2212＋5。

### N.3 我認為驗收方寫錯的地方

無。

### N.4 §6 六問的回答

**1.** 讓標準根節點算型別值：`is_type_value_combo`。牽動 `%super`（`@list.%super` 現在得 `@any`，先前是 `_`）、meet／`≤`（`@list & [1,2]` 仍走 List 校驗，不是欄位交集）。不牽動標準根位元組。手造開 combo 仍不獲 `%super`。E4 的 `@int` 短路仍在，因為它不在標準根上。

**2.** **`c596dc99…`（節點）**。不是第三個。沒有移動任何既有位址：標準根仍 `7038e250…`，`x: 0` 仍 `31745ef0…`。改變的是拼法 `@list` 指到哪一個已存在的值。

**3.** **進得了。** 提交 `v: @list` 後根裡是那顆節點（含 `%fmap`），物件數 **3**（節點內聯在使用者根裡，不另鑄 CAS 物件）。

**4.** **`@list = (@list)` 仍 `#true`**，現在是節點比節點。對照：`@list = {{ %kind: #type, %name: "list" }}` 現在是 `#false`（先前兩邊都是 marker，那一格是 `#true`）。

**5.** 全函數：

| 名字 | `@X` 是什麼 | `@X.f` |
| :-- | :-- | :-- |
| 標準根有節點的（`list`／`option`／`result`） | 那顆節點 | 節點上的欄位；沒有的欄（含未存的 `%super`）走衍生或 `_` |
| `SPEC_09` §2.1 其餘（`int`／`num`／`str`／`bool`／`any`／`float`／`combo`／`record`／`unit`／`morphism`／`type`／`caid`／`complex`／定寬整數） | 兩欄 marker | marker 的欄；`%fmap` → `_` |
| 不在樹裡的（`zzz` 與任何未知名） | 同樣 marker（開放） | 同上 |

沒有落點的欄 → `_`（開 miss）。沒有落點的名字 → 仍是型別值，只是沒有節點欄。

**6.** 見 N.3。

### N.5 規格側的發現（不要自己改）

`SPEC_09` §2.1／§2.5 兩張表仍是驗收方結案。`@list` 印出來現在帶 `%fmap`；`REAL_01` §1.3「印出必須是合法 n/」仍夠不到「讀回同一個值」，但讀回來的 `%fmap` **不再是 `_`**——那個缺口對標準根三個節點合上了。

### N.6 我沒做的事

`%super` 的樹與語義（G2）。`@zzz` 的開放性。D3′／D4。規格正文與那兩張表。本弧探針。標準根重算。身分。

---

## 8. 驗收回合（驗收方，2026-09-16）

**結論：通過，零修補回合。** 一項發現要記帳，而它是**驗收方措辭的錯**，見 §8.3。

### 8.1 通過的部分

| 檢查 | 結果 |
| :-- | :-- |
| diff 純度 | ✅ `eval.rs`／`lib.rs`／`type_constraint.rs`／`unify.rs` ＋ 本工單 §N |
| 探針完整性 | ✅ `git diff 6f461f4..2644099 -- crates/oo/tests/` **0 行** |
| 本弧探針 | ✅ **5／5**（3 綠 2 紅 → 全綠） |
| 全工作區 ×3 | ✅ **232 targets／2217 passed／0 failed**，三輪皆 `cargo_rc=0`（驗收方獨立跑） |
| conformance | ✅ **162／162** |
| 身分紅線 | ✅ `31745ef0…`／`7038e250…`／物件 3／`layout=5`／`encoding=5`；known-answer 帶對照 |
| 跨版本 | ✅ 本版寫 → `v0.50.0` 讀 rc=0 |

**I1 驗收方獨立複驗，且比探針寬**〔量，**12 名字 × 4 欄位 ＝ 48 格**，探針只測 2 欄〕
**不一致 0／48**；另造探針沒測過的三欄（`%id`／`%morphism`／`%nosuch`）亦逐格相同。

**I2 亦比探針深**〔量〕：`m.a.b.%fmap`（兩層 combo）與 **`l.0.%fmap`（list 的元素）**
皆得節點——**型別現在連在容器裡都帶得走。**

**兩處腳本看得見的行為變更，帶 `v0.50.0` 對照逐格複驗**：

| | v0.50.0 | 本版 |
| :-- | :-- | :-- |
| `@list.%super.%name` | `_` | **`"any"`** |
| `@list = {{ %kind: #type, %name: "list" }}` | `#true` | **`#false`** |
| `@list = (@list)` | `#true` | `#true`（現在是節點比節點） |
| `1 & @int`／`@int & "x"` | `1`／`_\|_` | **未變**（E4 的短路仍在） |

### 8.2 §6 六問的複核

**1**（讓節點算型別值）✅ `is_type_value_combo`，而 G2 第三格（手造開 combo 拿不到 `%super`）仍綠。
**2**（CAID）✅ **`c596dc99…`，既有節點，不是第三個位址**。
**3**（進根）✅ 進得了，物件仍 3（節點內聯）。
**4**（`=`）✅ 已於 §8.1 逐格複驗。
**5**（全函數）✅ 48 格 ＋ 三個另造欄位。**6** ✅。

### 8.3 ⚠ 一項發現，而它是驗收方的措辭錯

**紅線 R1 的抬頭寫的是「非紀元」，而它的內容是一張 digest 清單。清單全部守住了，
抬頭沒有。**

〔量，驗收方另造，帶 `v0.50.0` 對照〕**一個把型別當值存起來的使用者根，位址移動了**：

```
v: @list  提交後的根    v0.50.0  5debf7de…
                        本版     b8a028d5…     ⟹ 移動
```

**這不是缺陷，是 D68 的必然**——`@list` 從 marker 變成節點，凡是存過它的值就換了內容。
**取乙 就一定要付這個。** 錯的是**工單把不變式寫成了機制**（一張 digest 清單），
於是紅線讀起來像「什麼都不會動」，而它其實只保證了那五個數字。
**⟹ 這正是常設回饋「射程要寫不變式不是機制」的又一次付款，而這次是在紅線上。**

**射程量出來了，而它很窄**〔量〕：conformance 語料、引擎測試、`examples/`
**三處把型別當值存的檔案皆為 0** ⟹ **真紀元，但已知實例為零。**

**⟹ 對切版的後果（驗收方記帳，不需交付方動作）**：本弧的 CHANGELOG 必須有一條
**破壞性**條目，逐字說明**凡存過型別值的宇宙，其根位址改變**；
不得只寫「`@X.f` 的答案變了」。
