# A fallback that wins — 工單

> Queue ID **Q-027**。基線：引擎 `v0.23.1`（`1333340`）／規格 `v0.23.0-draft.1`。
> 偵察併入本檔 §1。裁定 **A／B** 已由用戶給出（§2），開單前無其他待裁。
>
> **明確不做**：`unwrap_or` 這個寫法本身（全樹 53 處），只治**決策位**的四處；
> `capacity` 與 `hops`（實測不進任何決策）；`.oo/` 以外的耐久路徑。

---

## 1. 量到的（偵察）

### 1.1 同一個寫法，在同一個檔案裡往兩個相反方向倒

〔量 2026-08-16，v0.23.1〕`crates/interpreter/src/peers.rs` 的耐久記錄解析中，
每一個欄位都用同一個慣用寫法 `…and_then(as_T).unwrap_or(X)`。逐項追它的後果：

| 欄位（位置） | 取不到時退回 | 後果 | 方向 |
| :--- | :--- | :--- | :--- |
| `ttl`（`:201`） | `0` | 〔量〕`oodp.rs:1074` `ttl == 0` ⟹ **不轉發** | **輸** ✅ |
| `services`（`:190`） | 空陣列 | 不匹配任何服務查詢 | **輸** ✅ |
| `observed_host`（`:205`） | `""` | `addr` 成為空字串 ⟹ 不可撥 | **輸** ✅ |
| `listen_port`（`:198`） | `0` | `addr` 成為 `host:0` ⟹ 不可撥 | **輸** ✅ |
| `provenance`（`:223`） | `Unknown` | **帶註解**「never promote to direct」 | **輸** ✅ |
| `capacity`（`:199`） | `0` | 〔量〕未進任何排序（§4.2.4 明文 MUST NOT 加權） | 中性 |
| `hops`（`:209`） | `0` | 〔量〕只被儲存與回顯，不進任何決策 | 中性 **⚠ 見下** |
| **`ts`（`:200`）** | `0` | 餵給 `received_at`；`0` ＝ epoch ⟹ **排最前** | **贏** ❌ |
| **`received_at`（`:210`）** | `ts` | 它是 `cmp_admission_order` 的**主鍵**（`:62`） | **贏** ❌ |
| **`admission_seq`（`:230`）** | `0` | **次鍵**（`:63`）；`0` 在一切真實序號之前 | **贏** ❌ |

> **⚠ `hops` 是潛伏的一項，本弧不動但必須記下。** `PeerAdvert::hops` 的文件逐字寫著
> 「`0` for a direct `#advertise`」——**那個退回值有語義**。今天〔量〕它不驅動任何決策
> （direct／relayed 由 `provenance` 承載，而那一處已刻意退回 `Unknown`），故歸為中性；
> 但**一旦有人開始讀 `hops`，它就會從「中性」變成「贏」**——一筆解析失敗的記錄會看起來像是直達的。
> **本弧不改它**（改了就要動身分以外的語義），但收弧時須入 Inbox。

**沒有人選擇過方向。** `provenance` 那一處是**唯一**被刻意選過的——它有一句註解說明
為什麼往「輸」的方向倒。其餘是同一個寫法碰上不同欄位的語義，**結果由巧合決定**。
這正是「**意外的性質不是性質**」（REAL_02 §4.3.5.1 自己的話）。

### 1.2 兩處已對現版重現

**〔量，行為級〕`admission_seq`**：5 個候選、3 個席位（`AUTOMATIC_REMOTE_CAP`）、
`received_at` 全部相同，只以 `admission_seq` 區分——一筆**缺席**、一筆**不可解析**
（`"abc"`）、三筆真實序號 6／7／8。**seated = [0, 1, 2]** ⟹
**那筆不可解析的記錄拿到席位，排在帶真實序號 7 與 8 的記錄之前。**
違反 REAL_02 §5.1.2「缺席與不可解析不是同一件事（MUST）／不得以同一個退回值涵蓋兩者」。

**〔量，碼級＋模擬〕熵失敗**：`oodp.rs:1143` 逐字為
`if rng.fill(&mut buf).is_err() { return 0; }`，註解寫著 `treat as 0`。
把 `sample_uniform_cap`（`:1115`）逐行轉寫後模擬：回 0 ⟹ `j = i + 0 = i` ⟹
`swap(i,i)` 對每個 `i` 皆為 no-op ⟹ `truncate(k)` 得**目錄序的前 k 個**
（模擬：20 筆目錄回 `peer0…peer7`）。違反 §4.3.5.1「不得靜默退回固定序（MUST NOT）」。

**且比它所修的舊缺陷更糟**：舊缺陷是**雜湊表迭代序**（逐行程種子，重啟即換）；
現行退回的是**耐久到達序，跨重啟穩定** ⟹ 落入該節自己的論證
「決定性的規則，是攻擊者也能離線算的規則」。
**可達性未證實**（Linux `getrandom` 開機後幾乎不失敗）——但該 MUST NOT 講的是
**不得這樣做**，碼裡就這樣做了；可達性是另一軸。

---

## 2. 主張與裁定

**主張：當引擎說不出一筆資料是什麼時，那筆資料不得因此獲勝。**

**裁定 A（用戶，2026-08-16）**：排序欄位**存在但不可解析** ⟹ **降到最後，記錄仍保留**。
理由：§5.1.2 把這件事框在**排序**而非**有效性**上；且已簽名的那一半可能完全完好，
壞的只是收方自己的斷言半邊——對等點不該因為我們自己的紀錄壞掉而消失。

**裁定 B（用戶，2026-08-16）**：熵取不到 ⟹ **查詢失敗並具名回報**，不回任何樣本。
理由：§4.3.5.1 自己的論證是「把某一次任意截斷擺得像正準答案，那才是偽造」——
回一個假的樣本比不回答更糟。

---

## 3. 射程（六處，已逐一分類）

> **本節逐項列出呼叫點，是 Q-026 賺到的規則**：工單若要求改動一個被到處使用的寫法，
> **必須自己先列出呼叫點並標註用途**，不得寫「把所有 `unwrap_or` 都修掉」。
> §1.1 的表即為完整清單；**表中標「輸」與「中性」者一律不動**。

### 3.1 缺席與不可解析必須可分（`peers.rs`，三處）

`ts`（`:200`）、`received_at`（`:210`）、`admission_seq`（`:230`）。

*   **缺席**：維持今日行為（舊引擎寫的記錄，得排在前）。
*   **不可解析**：**排在所有可解析記錄之後**（裁定 A），**且記錄仍載入、仍可被撥號**。
*   **不得以值域內的一個合法值代表「不可解析」**（例如 `u64::MAX`）——那是把一個
    哨兵塞進資料值域，與 `.oo/format` 的裸數字、CAS 物件自陳編碼同族。
    **應以獨立的欄位或型別承載該事實**（拼法不限）。
*   `cmp_admission_order`（`:61`）須據此排序；**它是 `pub`，簽章可改**。

### 3.2 熵失敗必須具名失敗（`oodp.rs`，一處）

`random_below`（`:1133`）取不到隨機性時**不得回 0**；`sample_uniform_cap`（`:1115`）
須能把該失敗往上傳，`#discover` 的處理（`:1090`）據此**回一個非 `#success` 的回應**。

*   **`%reason` 不得重用 `#malformed` 或 `#rejected`**——〔量〕現有詞彙為
    `#conflict／#malformed／#rejected／#stack_overflow／#success`，前兩者都是
    **請求方錯誤**，而這是**實作說「我做不到」**，屬 `#stack_overflow` 那一族
    （ERROR_CODES §2.7.3 已立此分野）。**須新增一個標籤**；
    **最終拼法於規格收尾定案，探針只釘性質不釘字面**。
*   **不得**在此路徑上退回任何樣本，亦**不得**回一個空的 `#success`
    ——那會與「目錄真的是空的」無法區分。

### 3.3 隨機來源必須可注入（否則 §3.2 無法被測）

〔量〕`SystemRandom::fill` 在 Linux 上幾乎不會失敗，**故該失敗路徑今天沒有任何測試
到得了**。本弧**必須**讓 `sample_uniform_cap`／`random_below` 的隨機來源成為參數
（trait 或閉包皆可），使一個**必定失敗的來源**可由測試注入。

> **⚠ §3.2 與 §3.3 在本弧的探針檔中無探針。** 整合層到不了熵失敗路徑。
> **交付方須自行附上一支單元測試**（注入必定失敗的來源，斷言查詢具名失敗且不回樣本），
> 放在 `crates/interpreter/` 內；驗收方會檢查它存在、且**移除該來源注入後它會紅**。
> **不要因為探針檔裡沒有它就跳過 §3.2／§3.3。**

---

### 3.4 客戶端讀不懂 `%status` 時，不得說成對方拒絕（**2026-08-16 加入，裁定 D**）

〔量〕`oodp.rs` 兩處以 `.and_then(|v| v.as_str()).unwrap_or("#conflict")` 讀 `%status`，
**而兩處的觸發條件不同**（分類由驗收方做完）：

| 位置 | 函式 | `%status` **缺席** | **存在但非字串** |
| :--- | :--- | :--- | :--- |
| `:1206` | `process_discover_reply` | **落到 `#conflict`** ❌ | **落到 `#conflict`** ❌ |
| `:1654` | `remote_fetch_oodp` | 上游守衛擋掉，走 `legacy_or_fail` ✅ | **落到 `#conflict`** ❌ |

**為何是假陳述**：〔量〕REAL_02 的表逐字規定 `#conflict` + 非 `#caid_mismatch` 之理由
（或無理由）⟹ 客戶端記 **`#peer_refused`**（「對等點拒絕受理」）。
⟹ **讀不懂對方說什麼，被記成對方拒絕服務我。**

**正確答案規格已經有**：同一張表的另一列——**本引擎不認得的 `%status` ⟹
`#peer_unknown_status`**，且**不得記入完整性紀錄**，理由逐字為
「**一個比你新的節點不是一個被弄壞的節點**」。

*   兩處**必須**改為 `#peer_unknown_status`（引擎已有 `BottomCause::PeerUnknownStatus`，
    `oodp.rs:1677` 已在用它處理**已知形狀但未知標籤**的情況——本項只是把
    **讀不出形狀**的情況併到同一個誠實答案）。
*   **不得**改成 `#rejected`——那同樣是一句關於對方**意願**的陳述。

> **⚠ `:1654` 在本弧的探針檔中無探針**——`remote_fetch_oodp` 需要真的網路對端。
> **驗收方會以既有的假 peer harness 實測**（`peer_fetch_verification_probe_test` 的形式），
> 送一個 `%status` 為數字的回應。**不要因為探針檔裡沒有它就只改 `:1206`。**
---

## 4. 已校準探針

檔案：`crates/interpreter/tests/a_fallback_that_wins_probe_test.rs`。
**不需簽章、行程或埠**——〔量〕`peers::load` **不驗簽**（驗簽是另一個
`verify_loaded` 步驟），且 `load`／`cmp_admission_order`／`directory_path`／
`FORMAT_TAG` 皆為 `pub`，故探針直接手寫目錄檔再呼叫真正的解析與比較器。

**探針修改權在驗收方。** 交付**只得**移除 `#[ignore]`，**不得**改動本檔任何其他位元組。

| 探針 | 斷言 | 基線 |
| :-- | :--- | :--- |
| **C0** | 控制組，**首位**：三筆可解析的 `admission_seq` 依序排列 | 綠（必須維持） |
| **C1** | 控制組：**缺席**的 `admission_seq` 仍排在前（舊記錄不得被本弧降級） | 綠（必須維持） |
| **P1** | **不可解析**的 `admission_seq` 排在**所有**可解析記錄之後 | **紅**：今天與缺席併成 `0`，排最前 |
| **P2** | **不可解析**的 `received_at` 同上 | **紅**：今天退回 `ts` |
| **P3** | 帶不可解析欄位的記錄**仍然被載入**（裁定 A 的另一半） | 綠（必須維持）——防止交付以「丟掉」來滿足 P1／P2 |

**P3 為何存在**：P1／P2 單獨可被「把壞記錄整個丟掉」滿足，而那**違反裁定 A**。
P3 把它釘死在「降級但保留」。

**C0 為何在首位**：其餘四支都在斷言「某個目錄載入後的順序」。若目錄根本載不進來，
全部真空。

---

## 5. 交付方自檢（**跑完這五項才算做完，探針全綠不是完成訊號**）

> **⚠ 本弧有一半的射程沒有探針。** §3.2／§3.3（熵）在探針檔裡**測不到**，
> 所以「五支探針全綠」**不代表本弧完成**——它只代表 §3.1 完成。
> 這一節就是為了取代那個錯誤的停止訊號而存在。

1.  `cargo test --workspace --no-fail-fast`：
    **基線〔實測 2026-08-16，開單當下〕1927／0／2（197 套件）⟹ 交付後應為 1929／0／0。**
2.  **§3.1**：`grep -n "unwrap_or(0)" crates/interpreter/src/peers.rs` ⟹
    三個排序欄位（`ts`／`received_at`／`admission_seq`）不再使用它；
    `ttl`／`capacity`／`hops`／`listen_port` **仍在**（那四處是控制組，不得動）。
3.  **§3.2**：`grep -c "treat as 0" crates/interpreter/src/oodp.rs` ⟹ **0**。
    熵取不到時 `#discover` 回**非 `#success`**、帶一個**新的** `%reason`
    （不得是 `#malformed`／`#rejected`），且**不回任何樣本、也不回空的 `#success`**。
4.  **§3.3**：隨機來源已成為參數，且你附了一支單元測試注入**必定失敗**的來源。
    自問一句：**把那個注入拿掉之後，那支測試會不會紅？** 不會紅就等於沒測到。
5.  §1.1 表中標「輸」與「中性」的七處，`git diff` 逐一確認**未被改動**。
6.  **§3.1 的三處逐一自問**：`ts`／`received_at`／`admission_seq`，
    **每一處都有對應的探針嗎？** 第二輪即因 `ts` 無探針而漏掉一整處。
7.  **§3.4**：`grep -n 'unwrap_or("#conflict")' crates/interpreter/src/oodp.rs` ⟹ **0**
    （今天為 **2**）。兩處都改，**不是只改有探針的那一處**。

---

## 6. 驗收（驗收方執行，交付方不必做）

1.  五支探針、交付方的熵單元測試、全工作區 **`--no-fail-fast`** ×5、conformance、genesis。

    **基線〔實測 2026-08-16，開單當下，非計算〕：1927 passed／0 failed／2 ignored（197 套件）**
    ——那 2 支 ignored 即本弧的 P1／P2。**交付後應為 1929／0／0。**
    （**不得從上一張工單抄**——Q-026 即因抄了上一弧的**交付前**數字而預測錯 2。）
2.  §1.1 表中標「輸」與「中性」的七處**逐一確認未被改動**（`git diff` 檢視）。
3.  重跑 §1.2 的行為級重現（5 候選 3 席位），確認那筆不可解析的記錄**不再拿到席位**。
4.  熵：檢查注入點存在，且**把注入拿掉之後該單元測試會紅**（一支不可能紅的紅不是閘）。

---

## 7. 完成條件

*   五支探針全綠且 `#[ignore]` 全數移除；探針檔逐位元組未被交付方改動。
*   熵單元測試存在並綠，且其失敗來源為**注入**而非真實熵失敗。
*   `grep -n "unwrap_or(0)" crates/interpreter/src/peers.rs` ⟹ **僅剩「輸」與「中性」那幾處**
    （`ttl`／`capacity`／`hops`／`listen_port`），三個排序欄位不再使用它。
*   `grep -c "treat as 0" crates/interpreter/src/oodp.rs` ⟹ **0**。
*   `grep -c 'unwrap_or("#conflict")' crates/interpreter/src/oodp.rs` ⟹ **0**（今天為 2）。
*   熵失敗的回應為 **`%status: #rejected`** ＋ 一個**新的** `%reason`（裁定 C，§130 允許理由集增長），
    **不得**是 `#conflict`。
*   全工作區 `--no-fail-fast` 五次全同、零 ignored、零失敗。

---

## 8. 驗收第一輪 — 一半（2026-08-16）

**§3.1 完成且做對了；§3.2／§3.3 完全未動。**

〔量〕探針 **5/5 綠**，探針檔逐位元組未改（只少兩行 `#[ignore]`）。
`peers.rs` 以**獨立布林欄位** `received_at_unparseable`／`admission_seq_unparseable`
承載「不可解析」，**而非值域內的哨兵**——那是 §3.1 最不直覺的一條，交付方做對了。
〔量〕`grep -c "treat as 0" crates/interpreter/src/oodp.rs` ⟹ **1**（未動）；
`oodp.rs` 的差異只有兩行結構欄位初始化。

### 這一半沒做，主因是本工單的結構缺陷

**不是交付方沒讀。** §3.1 那條非顯然的要求做對了，代表 §3 被仔細讀過。
問題在**停止訊號**：

1.  **本工單漏了「交付方自檢」章節。** Q-025 有（「## 4. 交付方自檢（做完再回報）」），
    **Q-026 與本工單都沒有**——是驗收方在 Q-025 之後把它弄丟的。
    少了它，交付方唯一的「我做完了嗎」訊號就是探針。
2.  **而本弧是第一次有一半的射程沒有探針。** 前幾弧「無探針」的項目都只是**一項**，
    其餘有探針兜底；本弧的 §3.2／§3.3 是**整整一半**。
3.  **完成條件被放在一個寫著「交付方不必做」的標題之後。**
    原順序為 §5 驗收（**驗收方執行，交付方不必做**）→ §6 完成條件。
    **交付方的判準，在一個告訴他可以停下的告示的另一側。**

⟹ **已修**：新增 §5「交付方自檢」置於驗收之前，標題逐字寫明
「**探針全綠不是完成訊號**」，並在其中複述熵那半的四項判準。

⟹ **常設規則追加兩條**：
*   **「交付方自檢」是必備章節，不是可選的**——它是交付方唯一的停止訊號的正本。
*   **完成條件不得放在標題寫著「交付方不必做」的章節之後。**
    讀者的停止點不在驗收方假設的地方。

### 第二輪要跑的

§3.2／§3.3 完成後：五支探針、交付方的熵單元測試（含「拿掉注入會不會紅」自問）、
全工作區 `--no-fail-fast` ×5（**應為 1929／0／0**）、conformance、genesis，
並逐一確認 §1.1 表中「輸」與「中性」的七處未被改動。

---

## 9. 驗收第二輪 — 兩件代修（2026-08-16）

熵那半**做對了**：`RandomSource` trait 使來源可注入，`random_below`／`sample_uniform_cap`
改回 `Result` 並向上傳，`serve_discover` 早退而不回任何樣本。
〔量〕`grep -c "treat as 0"` ⟹ **0**；單元測試存在，且**把注入換成真實來源後它會紅**
——它是閘（§5.4 的自問已由驗收方代跑）。排序那半的做法也正確：
`is_some() && as_T().is_none()` 正確分開「缺席」與「不可解析」，以**獨立布林欄位**承載，
不是值域內的哨兵；不可解析成為**主鍵**故排最後，且記錄仍保留（裁定 A）。探針 5/5。

### D1 — `ts` 那一處沒做，而它是工單列的三處之一

〔量，2026-08-16〕一筆 `ts: "abc"` 且 **`received_at` 缺席**的記錄，
與兩筆正常記錄一起載入，排序結果為 **`["9", "2", "3"]`**——**它仍排第一**。

成因：`received_at_unparseable` 的判準是「`received_at` **存在**且不可解析」，
而此處 `received_at` 是**缺席**的；於是 `ra` 退回 `ts`，而 `ts` 自己又 `unwrap_or(0)`
⟹ **epoch** ⟹ 排最前，**兩個旗標都沒有被設**。

**新增探針 P4**（驗收方於本輪加入，基線紅，失敗訊息即上述順序）。
**它之所以能活過一輪，正是因為 §3.1 列的三處裡只有兩處有探針。**
⟹ **常設規則追加**：**射程列了 N 處，探針就必須逐處對應**；
少一處，那一處就會以「其餘全綠」的形式通過。

### D2 — 回應說錯了話，而這正是本弧要修的那個病

熵失敗時回 `refuse(OodpStatus::Conflict, "entropy_unavailable", …)`。
`%reason` 是新的 ✓，但 **`%status` 用了 `#conflict`**，而：

*   〔量〕`#conflict` 在本檔的既有用途全是**請求方錯誤**：`malformed`、
    `unparseable_caid`、`missing_field`、`caid_mismatch`。
*   〔量〕ERROR_CODES 定義 `#conflict` ＝「**靜態邏輯不相容**」。
*   〔量〕**ERROR_CODES §`#peer_refused` 逐字規定**：對端回 `#conflict` 而其 `%reason`
    **非** `#caid_mismatch` 時，詢問方記為 **`#peer_refused`**（「對等點拒絕受理」）。

⟹ **詢問方會記下「那個節點拒絕服務我」，而事實是「那個節點的宿主取不到隨機性」。**
一個關於**對方意願**的假陳述——**與本弧要修的病完全同族**。

**本工單的責任**：§3.2 逐字寫的是「**`%reason` 不得重用 `#malformed` 或 `#rejected`**」，
而 `#malformed` 是 **reason**、`#rejected` 是 **status**——**我把兩個層級寫在同一句裡**，
於是「換一個新 reason、保留舊 status」完全符合字面。承重的是 **status**（那是對方據以
行動的詞彙），reason 只是附註。
⟹ **常設規則追加**：**約束一個分層的協定欄位時，必須逐層分別寫明**。

**待裁**：〔量〕`OodpStatus` 只有五個值（`Success`／`NotFound`／`Conflict`／
`NotImplemented`／`Rejected`），**沒有一個表示「我做不到」**。修法需要一個新的 status，
而那是**線上格式的擴充**，牽動 REAL_02 §3.2 與 ERROR_CODES ⟹ **交回用戶裁定後再做**。

### 驗收方在本輪弄壞過交付一次

驗收方為了驗「拿掉注入會不會紅」，以 `sed` 暫改 `oodp.rs` 後用
`git checkout crates/interpreter/src/oodp.rs` 還原——**那不是還原那次編輯，而是把整個檔案
退回已提交狀態**，交付方在該檔的全部未提交工作因此被清除；`git diff --stat` 回空被誤讀成
「還原成功」，其實它的意思是「與 HEAD 無差異」。**僅因完整 diff 恰好還在紀錄裡而得以逐字重建。**
⟹ **常設規則追加**：**驗收前先提交交付**；未提交的工作不得是唯一的副本，
而破壞性驗證一律在已提交狀態上做。本輪起交付於驗收**開始前**即提交（`3818533`）。

### 第三輪要跑的

D1（`ts`）修好；D2 待用戶裁定 status 後再修。屆時：六支探針（含 P4）、熵單元測試、
全工作區 `--no-fail-fast` ×5（**基線 1927／0／2 ⟹ 應為 1930／0／0**，本輪 P4 使總數 +1）、
conformance、genesis，並逐一確認 §1.1 表中「輸」與「中性」七處未動。

---

## 10. 裁定 C／D（用戶，2026-08-16）與其由來

**C — 熵失敗回 `#rejected` ＋ 新 `%reason`，不動規格。**
起因是驗收方原以為要新增一個 status，查證後發現**規格禁止**：
〔量〕REAL_02 §130 逐字——「**`%status` 集合不得為此增長（MUST NOT）**：仍為
`#success`／`#not_found`／`#conflict`／`#not_implemented`／`#rejected` **五者**。
新增 op 或新增可分情況**一律以 `%reason` 承載**。……理由集得增長；**狀態集不得**。」
⟹ `#rejected`（定義即「理解了但拒絕受理，區辨在 `%reason`」）＋新理由**正是該節的設計意圖**，
零規格變更。而 `#conflict`（「靜態邏輯不相容——**修你自己的封包**」）是假的。

**D — 客戶端那兩處 `.unwrap_or("#conflict")` 進本弧**（見 §3.4）。

**用戶另提的「跨版本相容條款」規格已有，且比 `#rejected` 更好**：
〔量〕REAL_02 的表已列「**本引擎不認得的 `%status` ⟹ `#peer_unknown_status`**，
**不記入完整性紀錄**」，理由逐字為「**一個比你新的節點不是一個被弄壞的節點**」；
`ERROR_CODES` 亦有同名條目；引擎 `oodp.rs:1677` **已經實作**。
⟹ **不需要新增條款**；本弧要做的是讓「讀不出形狀」的情況也走到那個已經正確的答案。

**用戶第 4 點（對使用者的表達應與引擎間的溝通分開）成立且已是現況**：
wire 層是 `%status`／`%reason`，使用者層是 `%cause`（`#peer_refused`／
`#peer_unknown_status`／`#peer_not_implemented`），後者已比前者細。
若日後要更細，該做的是**讓 `%cause` 也承載 `%reason`**，而不是撐大 wire 的狀態集。
**入 Inbox，不在本弧。**

---

## 11. 驗收第三輪 — 一件代修（2026-08-16）

**第二輪的兩件皆修好且驗證通過。**

| 項 | 結果 |
| :--- | :--- |
| 七支探針（含 P4／P5） | **7/7 綠**；探針檔逐位元組未動（只少兩行 `#[ignore]`）|
| §3.2 熵 | `refuse(OodpStatus::Rejected, "entropy_unavailable", …)` ⟹ 裁定 C ✓ |
| §3.4 `:1206` | 由 P5 覆蓋，綠 |
| §3.4 `:1654`（**無探針**） | **驗收方以真的假 peer 實測**：`%status` 為數字與 `null` 兩種， 皆得 **`PeerUnknownStatus`**（先前為 `PeerRefused`）✓ |
| `grep "treat as 0"` ／ `grep 'unwrap_or("#conflict")'` | **0** ／ **0** |
| conformance ／ genesis | **143/143** ／ **11/11** |

### D1 — `ts` 的修法過度降級，成為另一個方向的假陳述

修法把 `ts_unparseable` **無條件併進** `received_at_unparseable`。但
〔量〕`ra = o.get("received_at")…unwrap_or(ts)` ⟹ **`received_at` 只有在缺席時才退回 `ts`**；
它存在且可讀時，`ts` **完全不參與排序**。

〔量 2026-08-16〕一筆 `ts: "abc"` 而 `received_at` **完好且最早**的記錄，
與兩筆正常記錄同載，順序為 **`["2", "3", "9"]`** ⟹ **它被降到最後**。
**引擎知道它何時到達，卻報告「我不知道」。**

**方向上是安全的**（降級不是升級，沒有東西因此獲勝），故**不違反本弧的主張**；
但它是**另一個方向的假陳述**，而本弧的整個論旨正是「**值必須說真話**」。
⟹ 用戶 2026-08-16 裁定：**算代修。**

**修法（一行）**：`ts_unparseable` **只在 `received_at` 缺席時**傳染——
即只在 `ra` 真的退回 `ts` 的那條路徑上。

**新增探針 P6**（驗收方於本輪加入，基線紅，失敗訊息即上述順序）。
**P6 與 P4 不衝突**：P4 的 `received_at` **缺席**（排序鍵真的未知），
P6 的 `received_at` **存在**（真的已知）——本弧要做的正是**把這兩種情況分開**。

### ⚠ 本輪的全跑量測被驗收方污染，須重跑

〔量〕五次結果為 `1932/0/0`×3、`1932/0/1`×2。**那個 1 不是不穩定**——
驗收方在電池執行**期間**把 P6（帶 `#[ignore]`）寫進探針檔，run 4／5 重編譯後就看到了它。
⟹ **本輪的 ×5 不算數，第四輪須重跑。**
⟹ **常設規則追加**：**全跑進行中不得改動樹**；要加探針就等電池跑完，或先加再跑。

**順帶更正驗收方的一個口頭預測**：先前在對話中說「應為 1931」，實測為 **1932**——
漏算了交付方自己新增的那支熵單元測試。**基線與預期值都要量，不要算**（同 Q-026 教訓）。

### 第四輪要跑的

D1 修好後：**八支探針**（含 P6）、熵單元測試、全工作區 `--no-fail-fast` ×5
（**應為 1933／0／0**——本輪實測 1932 ＋ P6 一支；**驗收方會實測確認，不以此數為準**）、
conformance、genesis，並逐一確認 §1.1 表中「輸」與「中性」七處未動。
