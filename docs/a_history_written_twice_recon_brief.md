# 偵察工單：一份被寫了兩次的歷史

**Queue ID**：**Q-015**（`nlang-spec/meta/WORK_QUEUE.md` §3、Ready 表第 2 列；弧 A 的 **A2 ＋ A3**）。
**這是偵察，不是實作**（§8 開弧 3——本卡不只缺一則裁定，**它的卡面敘述本身已經過期**，見 §1.1）。
**基線**：`v0.40.0`，二進位 `/home/gali/nlang-baselines/v0.40.0-verify/target/release/oo`
（`oo --version` → `oo v0.40.0`；known-answer 已過：`oo run ka.n -o r`，`r: ~%Math./add (1,2)` → `3`）。
**你也必須先對你自己用的二進位做一次 known-answer**，再開始量。
`oo --version` 這一支**不知道自己是哪一版**（Inbox 三個實例），所以版本要用行為確認，不要只信自陳。

**規格依賴**：
`SPEC_10` §3.1（Savepoint 五款 ＋ 前驅款 D50 ＋ 產生判準 (c) D51）／
`SPEC_08` §6.2（特權審計歸屬 Commit；**§578 有一條 MUST 直接依賴 `Commit.parent` 存在**）／
`REAL_01` §4.2 參考佈局 **[Reference Recommendation]**／§7.3 審計日誌 **[Core Requirement]** ＋ 其 2026-07-27 適用條件註記。

**裁定依賴**：D43／D47（○ 是什麼、何時產生）／D50（前驅是複數）／D51（覆蓋關係改變即鑄）。
**`commit.md` §1.7.1 兩張圖**（本地時間序 ○ ／ 全球特化序 ⊑）／**§1.7.7**（`parent` 不再設定，代價為零）／
**§1.11**（歷史層不繼承值層的冪等性）。

---

## 1. 驗收方已量到的（你不必重量；但你可以推翻，推翻請帶指令）

### 1.1 卡面上的兩個名字，一個已經被裁掉了，一個沒有被定價

卡片逐字寫著「**W11：`wal/` ＋ `audit.log` logical log**」。〔讀 2026-08-31〕

*   **`audit.log` 那一半已經被裁掉了。** `REAL_01` §7.3 底下的 **2026-07-27 適用條件註記**逐字寫著：
    `.oo/audit.log` 與 §7.1／§7.2 同屬**服務面**；**於本機面，特權操作的審計面是 Commit**
    （`SPEC_08` §6.2），而不是工作區裡的一個檔——後者位於斷言層、無從驗證。
    並且**明文 MUST NOT**：「**不得**以 `.oo/audit.log` 取代 §6.2 之 Commit 標記」。
    ⟹ **「在本機面做一個 `audit.log`」不是一件可以交付的事，它是一件被規格禁止的事。**
*   **而 `REAL_01` §4.2 的參考佈局第 490 行仍然把 `audit.log` 畫在 `.oo/` 底下。**
    同一份文件的兩節互相矛盾，且矛盾已存在四個月。（§4.2 整節失真已在 Inbox 有一列。）
*   **`wal/` 那一半從來沒有被定價。** 全樹 grep：`nlang-tools/crates/` 對 `wal/`／`"wal"`
    **零命中**（唯一命中是上一弧探針的一行註解）。規格側只有 §4.2 那一行圖，**零條文**。

⟹ **本卡的第一件事不是實作，是重新定義它自己是什麼。**

### 1.2 ○ 與 ● 從不互相指涉——本卡最重要的一句

〔量 2026-08-30，上述標籤二進位〕`evolve x:1` → `commit c1` → `evolve y:2` → `commit c2` → `evolve z:3` → `commit c3`，
三個 ○ 檔逐字如下：

```
.oo/savepoints/3097eaac…   #nlang/store savepoint / parents:                    / { x: 1 }
.oo/savepoints/d781573f…   #nlang/store savepoint / parents: 3097eaac…          / { y: 2 }
.oo/savepoints/ce9e361c…   #nlang/store savepoint / parents: d781573f…          / { z: 3 }
```

而 `oo log` 走的是**另一條鏈**（`ccef99ce…` → `c54202d8…` → `5fce07a5…`），
每一顆帶 `message`、`Date`、`root`。

*   **○ 檔裡沒有任何一個 CAID。** 沒有 commit 的位址，沒有 root 的位址。
*   **`Commit` 結構裡沒有任何一個 ○ 的識別碼。**〔讀 `value.rs:2405`〕
    欄位是 `parent` / `root` / `meta` / `kind` / `refine_info`，**一個都不指向 ○**。

⟹ **兩張圖在磁碟上完全不相連。** `commit.md` §1.7.1 說「那是兩張圖，不是一張」——
今天兌現的是這句話的字面意思：**兩張圖，而且沒有任何一條邊把它們接起來。**

⟹ **A2 逐字所寫的那條路徑今天走不通。** §4.1 A2 說「歷史走訪須先改走 ○ 鏈」——
但 ○ 鏈裡**沒有歷史**：它裝的是每一刻的**工作集**（`{ z: 3 }`），不是宇宙，也不是提交。
`oo log` 今天印的四樣東西（commit CAID／message／Date／root）**在 ○ 裡一樣都沒有**。

### 1.3 ○ 是一條快照鏈，不是一份日誌——而它的成本是平方的

〔量〕連續 `oo evolve`（每次加一個相異欄位），單一倉，不提交：

| n（○ 數） | 單次 `evolve` 牆鐘 | `savepoints/` 總位元組 |
| :-- | :-- | :-- |
| 1 | **0.02 s** | 41 |
| 200 | **0.45 s** | **182,998** |
| 322 | **0.84 s** | **482,115** |

*   **位元組是平方的，已驗到 1.5%**：$(322/200)^2 = 2.59$，實測 $482115/182998 = \mathbf{2.63}$。
    成因〔量〕：**每一顆 ○ 存的是當下的整個工作集，不是差量**——第 1 顆 41 B（`{ f1: 1 }`），
    第 200 顆 1,857 B（`{ f1: 1 f10: 10 … f200: 200 }`，200 個欄位全在裡面）。
*   **單次成本超線性**：n=322 時是 n=1 的 **42 倍**。成因〔讀 `savepoint.rs:118`〕
    `record` 每次都 `load_nodes`（讀**全部**檔）以算 `tips_of` ⟹ **沒有 tip 指標**。
*   **400 次連續 `evolve` 在兩分鐘內跑不完**（逾時中止於第 322 次）。
*   **`commit` 不回收 ○**〔量〕：三次提交後 `injections/` 由 3 清為 **0**，`savepoints/` 維持 **3**。
    §3.1 耐久款要的正是這個（○ 必須在 Commit 之後繼續存在）⟹ **不是缺陷，是條文的直接後果**。

**⚠ 一個必須說清楚的限定**：**平方是「每個提交區間之內」的，不是全域的。**
提交會清掉 `injections/`，所以下一顆 ○ 只裝新的注入（實測 `{ y: 2 }` 而非 `{ x: 1, y: 2 }`）。
⟹ **檔案數無界成長是全域的；單檔大小的平方是區間內的。** 不要把這兩句合成一句。

⟹ **A3「日誌條目的正準形」不是一個命名問題。** 今天一個「條目」＝**一份完整快照**，
而日誌的定義是記**事件**。這張卡問的正是：一條記錄裡該有什麼。

### 1.4 `Commit.parent` 有六個讀者，其中一個是規格的 MUST

〔讀，全樹 grep，排除 `Path::parent`〕

| 位置 | 做什麼 | `parent` 消失後 |
| :-- | :-- | :-- |
| `lib.rs:4848` `Ouroboros::log` | 走鏈產出 `oo log` | `oo log` 只剩 HEAD 一列 |
| `universe.rs:1222` `commits_after` | squash 審計訊息要說它移除了幾顆 | 永遠 `Err("not an ancestor")` |
| `universe.rs:1250/1257` `squash` | 驗證 base 是 HEAD 的祖先 | **`squash` 永遠拒絕** |
| `universe.rs:1496/1542` refine 影子掃描 | 走鏈找受影響的 commit | 只看得見 HEAD |
| `main.rs:1597` `oo inspect` | 印 `parent:` | 恆印 `(none)` |
| `store_codec.rs:556/1551` | 編／解碼 | — |
| `value.rs:2432/3629` | 相等性；**進 CAID** | — |

**而 `SPEC_08` §578 是一條 MUST**，逐字：「`#squash` **必須**同時使區間內的 `parent` 邊與
**放棄邊**脫離可達鏈」。⟹ **A2 不是純引擎改動**：那條 MUST 以 `parent` 邊命名了它的射程。

> **⚠ 2026-08-31 驗收時就地更正（驗收方的錯，交付方 Q4 抓到）**：§578 的**下一段**就是
> **【2026-07-29 修訂，本條兩處已被 §6.2.1 之裁定推翻】**，逐字裁定**放棄邊不是可達性根**，
> 並把「必須同時斷開兩類邊」**予以保留但改其理由**。⟹ 上面那句結論仍然成立
> （要求仍在，且仍以 `parent` 邊命名射程），**但引用時只讀了 MUST 那一行、沒讀它下面的修訂**。
> 這正是本專案這幾週一直在別人身上指認的病。**grep 命中之後要往下讀完那一節。**

### 1.5 ○ 沒有任何 CLI 表面

〔量〕`oo --help` **十八**個子命令（**⚠ 2026-08-31 就地更正：原寫十七，少算了 `lint`**），**沒有一個看得到 ○**。`oo log` 是 ● 的 log。
〔讀〕`savepoint::load` 仍 `#[allow(dead_code)]`，`savepoint::` 在該檔之外的唯一呼叫者
是 `universe.rs:894`（`save_staged` 裡）⟹ **○ 層在 v0.40.0 之後仍然是唯寫的。**

⟹ 用戶 2026-08-30 那句「直覺會想著 `oo log` 的 tree 畫不出來」——**今天連畫都沒有地方畫。**

### 1.6 一個把 `CommitMeta` 加欄位的定價，比看起來便宜

〔讀 `value.rs:3626`〕`Commit::content_hash` 把 meta 以 **`format!("{:?}", self.meta)`** 進雜湊。
**但 `CommitMeta` 有一份手寫的 `Debug`**〔讀 `value.rs:2348`〕：`abandoned` 與 `privileged_effect`
**為 `None` 時不進 debug_struct** ⟹ 既有 commit 的 digest 逐位元組不動。
⟹ **照同一個模式加一個 `Option` 欄位，對舊 commit 是零成本的。**
**但**：若新欄位在**每一顆新 commit 上都是 `Some`**，則同一個宇宙由舊／新引擎提交會得到
**不同的 commit CAID**。那是不是紀元，是本偵察要報價的事之一（見 Q9）。

---

## 2. 你要回答的（每題都要 `檔案:行號` 或可重跑指令）

**⚠ 全域規則（§8 開弧 4）**：任何引用內建的讀數，**必須先證明那個呼叫發生了**——
換一個會改變答案的引數，或讀 `.%effect`。**`#pure` 的第一個假說永遠是「這個呼叫沒有發生」。**

### 2.1 A2：`parent` 退場的真實射程

**Q1.** 逐一對 §1.4 的六個讀者給出**行為證據**：寫一個最小倉，讓每一個讀者今天做出可觀測的事
（`oo log` 印幾列、`oo squash` 成不成功、`oo inspect` 印什麼、refine 影子掃描掃到幾顆），
然後說明「`parent` 恆為 `None`」之後那一格會變成什麼。**不要只讀原始碼。**

**Q2.** 除了 `Commit.parent`，磁碟上**還有沒有任何東西記得提交的先後**？
（`.oo/HEAD` 是不是只有一行？有沒有 reflog？`CommitMeta.timestamp` 的解析度是多少、
兩次相鄰提交會不會同秒？——上面 §1.2 那三顆的 `Date` 相差 40–60 ms，請量它的實際欄位精度。）
**這一題決定 A2 有沒有一條「不新增任何東西」的路。**

**Q3.** refine 影子掃描（`universe.rs:1496/1542`）**要的是可達性還是覆蓋關係**？
即：把 `parent` 鏈換成一個「祖先集合」而不畫邊，它還做得對嗎？
（`commit.md` §1.7.5 的判別器是「畫圖要覆蓋關係，裸偏序只給可達性」——這一題就是它的實例。）

**Q4.** `SPEC_08` §578 那條 MUST（squash 須斷開 `parent` 邊與放棄邊）**今天兌現嗎**？
量它：造一條有放棄邊的歷史，squash，然後看被放棄的 commit 還可不可達。
**這一題可能自己就是一列 Inbox**，與 A2 是否要動它是兩件事。

### 2.2 A3：一條記錄裡有什麼

**Q5.** ○ 的框今天裝得下哪些值？逐一測：`_`（Top）／`_|_`（Bottom 帶 `%cause`）／
**`Blur`（帶視界參數）**／`#incomplete`／帶 `%effect` 的值。
`encode_savepoint` ↔ `parse_savepoint_parents`／`decode_staged` 來回一趟，**位元組相同嗎**？
〔背景〕`observation_result.md` §1 的承重結論是「有三條軸，其中兩條被塞進一個槽」，
而 **A3 卡在那裡**——`#incomplete` 沒有 CAID，所以產生判準不能用 digest 表述（§1.10.5）。
**但 D51 已經給了一個不用 CAID 的判準（覆蓋關係改變）** ⟹ 請回答：**A3 還卡在 §1 上嗎？**

**Q6.** §3.1 產生判準 **(b)（觀測真的化約了一個 thunk）與 `_|_` 那一款，兌現度為零**
（Inbox 已有一列，2026-08-30 量）。那一列留了一個混淆因子未解：
**是「觀測路徑沒有寫 ○ 的入口」，還是「觀測路徑根本沒有倉庫可寫」（Q-018）？**
Q-014b 偵察的 Q6 已答「`run_one_shot` 的根是 `None`、REPL 走 `load_universe`」——
**請用那個答案把這一列的家定下來**：它屬 Q-015，還是屬 Q-018？帶指令。

**Q7.** 把 §1.3 的成本量再推一步：**在有提交的情況下**，○ 的檔數與總位元組怎麼長？
（每 10 次 evolve 提交一次，跑到 n=300，給表。）
並回答：**有沒有任何現有機制會回收 ○**？（`gc` 碰不碰 `savepoints/`？量它。）

**Q8.** 一條「日誌條目」的候選欄位有哪些是**今天就已經存在於某處**的？
逐項指出來源：事件種類（注入／觀測／commit——`commit.md` §1.10.7 的三個字）／
時間／作者／前驅集合／它產生的值或位址／視界參數（`HorizonParams`）／`%effect`。
**哪些今天有家，哪些沒有。**

### 2.3 定價（**不要選邊**）

**Q9.** 對下列四個候選各報一次價。**逐項給：動不動身分、動不動磁碟格式、要不要升 `.oo/format`
（今天仍是 `layout=2`，Q-013 新增目錄時沒有升版）、幾個檔、既有測試會紅幾支、跨版本讀相容如何。**

| | 候選 | 一句話 |
| :-- | :-- | :-- |
| **甲** | **A2 結案為「不做」** | ● 保留 `parent`，○ 保持現狀，本卡縮成 A3 一件 |
| **乙** | **第三份紀錄** | 新增一份 append-only 事件日誌，**● 與 ○ 都成為它的衍生視圖** |
| **丙** | **○ 長出提交邊** | ○ 記「我變成了 commit C」；● 的 `parent` 降為 deprecated；走訪把兩張圖接起來 |
| **丁** | **● 長出 ○ 邊** | `CommitMeta` 加一個 `Option` 欄位記來源 ○（定價見 §1.6，**先答那個跨版本問題**） |

**⚠ 逐字禁止**：不要在報價裡替 D50（前驅複數）與任何候選的撞車挑一邊。
若某個候選要求 ○ 與 ● 共用識別碼空間，那**牴觸 §3.1 身分款的 MUST NOT**（○ 的身分不得為 CAID），
那是回頭請裁的材料，不是實作可以決定的事。

**Q10.** 上面四個候選裡，**哪一個能讓 `oo log --graph` 畫得出來**（用戶 2026-08-30 的原始直覺）？
給出「要畫一張圖，最少需要哪些欄位」的清單，**然後說每個候選缺哪幾項**。

**Q11.** `wal/` 還需要嗎？枚舉今天 `.oo/` 底下**所有「寫兩個以上檔案、而中間崩機會留下不一致狀態」**
的位置。已知的一個：`universe.rs:1272` `put_commit` 然後 `:1273` `set_head`。
**逐個說崩機後的後果**（孤兒物件無害？還是真的不一致？），然後回答：
**這些窗口有哪一個是 `atomic_write` 單檔重新命名解不掉、非要 WAL 不可的？**
〔提醒〕`storage.rs` 的 `atomic_write` 最後一步是 `tmp.persist(path)` ＝ **覆寫式 rename**，
它的註解自己寫著這件事。上一弧就是在這裡撞牆的——不要重複那個誤判。

**Q12.** 本卡與 **Q-016**（meet 路徑 CAS ＋ 重試）的分界在哪？
Q-014b 已把 meet 那一半的表面積削掉一大塊。**如果乙／丙／丁的某一個順手把 Q-016 也解掉了，
那是報價的一部分，但不得據此把兩張卡合併**——合併要一則裁定。

---

## 3. 交付形式

*   一份 `nlang-tools/docs/a_history_written_twice_recon.md`，**逐題編號對齊 §2**。
*   每題帶 `檔案:行號` 或可重跑指令；**量到的數字要能被重跑重現**。
*   **不動任何產品程式碼。** 這是偵察。允許寫一次性腳本，跑完刪掉，樹要乾淨。
*   **答不出來的題目寫「答不出來，因為……」，不得留空、不得跳過。**
*   若你認為驗收方在 §1 哪一句寫錯了，**帶指令推翻它**——上一弧的 Q9 就是這樣把
    驗收方的 `O_EXCL` 建議整條推掉的，那次推翻是本協定當週最有價值的一件事。
